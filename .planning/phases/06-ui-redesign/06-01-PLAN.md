---
phase: 06-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/components/ui/SaasFooter.tsx
  - src/app/(public)/tos/page.tsx
  - src/app/(public)/privacy/page.tsx
  - src/app/(public)/contact/page.tsx
  - src/app/(public)/page.tsx
autonomous: true
requirements: [UI-01, UI-04]

must_haves:
  truths:
    - "Login and register pages show a premium card with fade-in + upward slide entrance animation"
    - "Login and register pages display on a subtle radial gradient background"
    - "Footer on landing page has multi-column layout with Product, Legal, Social, Contact sections"
    - "TOS link in footer navigates to /tos with Czech legal content"
    - "Privacy link in footer navigates to /privacy with Czech GDPR content"
    - "Contact link in footer navigates to /contact with a working form"
    - "Instagram icon in footer links to Instagram"
  artifacts:
    - path: "src/components/ui/SaasFooter.tsx"
      provides: "Multi-column SaaS footer component"
      min_lines: 60
    - path: "src/app/(public)/tos/page.tsx"
      provides: "Czech TOS page"
      min_lines: 40
    - path: "src/app/(public)/privacy/page.tsx"
      provides: "Czech GDPR/privacy policy page"
      min_lines: 40
    - path: "src/app/(public)/contact/page.tsx"
      provides: "Contact page with form"
      min_lines: 50
  key_links:
    - from: "src/components/ui/SaasFooter.tsx"
      to: "/tos, /privacy, /contact"
      via: "Next.js Link components"
      pattern: "href=.*/tos|href=.*/privacy|href=.*/contact"
    - from: "src/app/(public)/page.tsx"
      to: "src/components/ui/SaasFooter.tsx"
      via: "import and render"
      pattern: "import.*SaasFooter"
---

<objective>
Add entrance animations to auth pages and build the expanded SaaS footer with three new legal/contact pages.

Purpose: Auth pages get the premium feel (CONTEXT.md: centered card, gradient bg, Framer Motion fade-in + slide), and the footer becomes a proper SaaS product footer with legal links, social media, and contact info. Three new static pages provide the content those links point to.

Output: Polished auth page entrance, SaasFooter component, /tos page, /privacy page, /contact page
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-redesign/06-RESEARCH.md

<interfaces>
<!-- UI primitives available via barrel export -->
From src/components/ui/index.ts:
```typescript
export { Button, type ButtonProps } from './Button';
export { Card } from './Card';
export { Input, type InputProps } from './Input';
export { Textarea, type TextareaProps } from './Textarea';
export { Select, type SelectProps } from './Select';
export { Badge, type BadgeProps } from './Badge';
export { FormInput } from './FormInput';
export { FormTextarea } from './FormTextarea';
export { FormSelect } from './FormSelect';
```

From src/components/ui/Button.tsx:
```typescript
export const buttonVariants = cva(...); // For Link-as-button styling
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add entrance animation to auth pages (login + register)</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx</files>
  <action>
Both auth pages already have the gradient background and Card layout from Phase 5. The changes are:

1. Add `'use client'` directive if not already present (needed for Framer Motion).
2. Import `{ motion }` from `'framer-motion'`.
3. Wrap the existing card container `<div className="w-full max-w-md">` with a `motion.div`:
   ```tsx
   <motion.div
     className="w-full max-w-md"
     initial={{ opacity: 0, y: 20 }}
     animate={{ opacity: 1, y: 0 }}
     transition={{ duration: 0.4, ease: 'easeOut' }}
   >
     <Card className="shadow-lg">
       {/* existing content unchanged */}
     </Card>
   </motion.div>
   ```
4. Keep the existing radial gradient background style on the outer div:
   ```
   style={{ background: 'radial-gradient(ellipse at top, var(--color-secondary) 0%, var(--color-background) 70%)' }}
   ```
   If not present, add it.
5. Upgrade error banner to use border-l accent style:
   ```tsx
   <div className="border-l-2 border-red-400 bg-red-50/70 text-red-700 px-4 py-3 rounded-r-lg text-sm mb-4">
   ```
6. Apply the same changes to both login and register pages. Both should look identical in structure.

Do NOT change the form logic, OAuth buttons, or any functional behavior. This is visual only.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -l "motion.div" src/app/\(auth\)/login/page.tsx src/app/\(auth\)/register/page.tsx | wc -l | grep -q "2" && echo "PASS: Both auth pages have motion.div" || echo "FAIL"</automated>
  </verify>
  <done>Both login and register pages render with fade-in + upward slide entrance animation using Framer Motion, on a radial gradient background, with upgraded error banner styling.</done>
</task>

<task type="auto">
  <name>Task 2: Create SaasFooter component and three new pages (TOS, privacy, contact)</name>
  <files>src/components/ui/SaasFooter.tsx, src/app/(public)/tos/page.tsx, src/app/(public)/privacy/page.tsx, src/app/(public)/contact/page.tsx, src/app/(public)/page.tsx</files>
  <action>
**SaasFooter.tsx:**
Create `src/components/ui/SaasFooter.tsx` as a new component (keep existing `Footer.tsx` for the public wedding page):

1. Multi-column layout: 4 columns on lg, 2 on sm, 1 on mobile.
2. Columns:
   - **Brand**: Svoji logo/wordmark, short tagline ("Plánování svatby s AI asistentem"), copyright with current year.
   - **Legal**: Links to /tos ("Obchodní podmínky") and /privacy ("Ochrana soukromí").
   - **Social**: Instagram icon (from lucide-react `Instagram`) linking to `https://instagram.com/svoji.cz` (placeholder).
   - **Contact**: Link to /contact ("Kontaktujte nás"), email placeholder (info@svoji.cz).
3. Styling:
   - Background: `bg-[var(--color-text)]` (dark) with white text.
   - Bottom bar: `border-t border-white/10 pt-8 text-center text-sm text-white/50` with `© {year} Svoji.cz`.
   - Use `container` class for max-width alignment.
   - Padding: `py-12 lg:py-16`.
4. Use Next.js `Link` for internal links, `<a>` for external.
5. This is NOT a `'use client'` component -- it's static, no interactivity.

**Landing page update:**
In `src/app/(public)/page.tsx`, replace the existing `LandingFooter` import with `SaasFooter`. Import from `@/components/ui/SaasFooter`. Keep the existing LandingFooter file in case it's used elsewhere but the landing page should use SaasFooter.

**/tos page:**
Create `src/app/(public)/tos/page.tsx`:
- Server component (no `'use client'`).
- Czech TOS content with standard sections: Úvodní ustanovení, Registrace a účet, Podmínky užívání, Odpovědnost, Ochrana osobních údajů (link to /privacy), Změny podmínek, Závěrečná ustanovení.
- Use Cormorant Garamond heading font via `font-heading` class for h1/h2 headings.
- Container layout with readable max-width (`max-w-3xl mx-auto`).
- Import and render `SaasFooter` at the bottom.
- Set page metadata: `export const metadata = { title: 'Obchodní podmínky | Svoji.cz' }`.

**/privacy page:**
Create `src/app/(public)/privacy/page.tsx`:
- Same structure as TOS.
- Czech GDPR privacy policy content: Správce údajů, Jaké údaje zpracováváme, Účel zpracování, Právní základ, Doba uchovávání, Vaše práva (přístup, oprava, výmaz, přenositelnost), Cookies, Kontakt.
- Metadata: `export const metadata = { title: 'Ochrana soukromí | Svoji.cz' }`.
- Import and render `SaasFooter` at the bottom.

**/contact page:**
Create `src/app/(public)/contact/page.tsx`:
- `'use client'` -- needs form interactivity.
- Use `react-hook-form` + `zod` for validation (same pattern as RSVP.tsx).
- Fields: Jméno (name), Email, Zpráva (message, textarea).
- Validation: name required (min 2 chars), email required + valid, message required (min 10 chars).
- Use `Input`, `Textarea`, `Button` from `@/components/ui`.
- On submit: open `mailto:info@svoji.cz` with subject and body pre-filled as fallback. Show success message "Děkujeme za vaši zprávu! Brzy se ozveme." after "submit".
- Card wrapper around the form for visual consistency.
- Import and render `SaasFooter` at the bottom.
- Add entrance animation (motion.div fade-in) on the form card.

All three pages live in the `(public)` route group so they inherit page transitions from template.tsx.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && test -f src/components/ui/SaasFooter.tsx && test -f src/app/\(public\)/tos/page.tsx && test -f src/app/\(public\)/privacy/page.tsx && test -f src/app/\(public\)/contact/page.tsx && grep -q "SaasFooter" src/app/\(public\)/page.tsx && echo "PASS: All files exist and landing page uses SaasFooter" || echo "FAIL"</automated>
  </verify>
  <done>SaasFooter renders multi-column layout with Product/Legal/Social/Contact columns. TOS, privacy, and contact pages exist with Czech content. Landing page uses SaasFooter instead of LandingFooter. Contact page has validated form with mailto fallback.</done>
</task>

</tasks>

<verification>
1. Auth pages: Visit /login and /register -- card should fade in with upward slide animation on load.
2. Footer: Visit landing page (/) -- footer should show 4-column layout with links to /tos, /privacy, /contact and Instagram icon.
3. TOS: Visit /tos -- should show Czech TOS content with proper typography.
4. Privacy: Visit /privacy -- should show Czech GDPR content.
5. Contact: Visit /contact -- should show form with validation. Submit should show success message.
6. All new pages should have page transitions when navigating between them (inherited from (public) template.tsx).
</verification>

<success_criteria>
- Auth pages have Framer Motion entrance animation (fade + slide up)
- SaasFooter component exists with 4-column layout
- /tos, /privacy, /contact pages exist with Czech content
- Landing page renders SaasFooter
- Contact form validates inputs and shows success state
- All pages use design system tokens (OKLCH colors, Cormorant Garamond headings)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-redesign/06-01-SUMMARY.md`
</output>
