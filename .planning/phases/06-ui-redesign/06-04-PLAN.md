---
phase: 06-ui-redesign
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - src/components/sections/Hero.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Gallery.tsx
  - src/components/sections/Timeline.tsx
  - src/components/sections/Locations.tsx
  - src/components/sections/Contacts.tsx
  - src/components/sections/RSVP.tsx
  - src/components/ui/Navigation.tsx
  - src/app/(public)/w/[slug]/page.tsx
autonomous: false
requirements: [UI-03]

must_haves:
  truths:
    - "Hero section has parallax background movement on scroll (desktop only)"
    - "Gallery section has parallax depth effect on scroll (desktop only)"
    - "All sections use OKLCH design tokens for colors and typography"
    - "All sections have scroll-triggered entrance animations"
    - "RSVP form is wrapped in Card with styled radio buttons"
    - "Navigation shows couple names dynamically"
    - "Parallax is disabled on mobile (< md) and when prefers-reduced-motion is set"
    - "A guest visiting /w/[slug] sees a visually premium page"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Hero with parallax background layer"
      contains: "useScroll"
    - path: "src/components/sections/Gallery.tsx"
      provides: "Gallery with hover effects and staggered entrance"
      contains: "useScroll"
    - path: "src/components/sections/RSVP.tsx"
      provides: "RSVP form with Card wrapper"
      contains: "Card"
    - path: "src/components/ui/Navigation.tsx"
      provides: "Navigation with couple name prop"
      min_lines: 30
  key_links:
    - from: "src/app/(public)/w/[slug]/page.tsx"
      to: "src/components/ui/Navigation.tsx"
      via: "import with couple name prop"
      pattern: "Navigation"
    - from: "src/components/sections/Hero.tsx"
      to: "framer-motion"
      via: "useScroll + useTransform for parallax"
      pattern: "useScroll|useTransform"
---

<objective>
Apply parallax scroll effects, design token treatment, and visual polish to all public wedding web sections, plus a visual verification checkpoint.

Purpose: The public wedding page (/w/[slug]) is what guests see -- the product showcase. CONTEXT.md requires high polish with parallax scroll effects on Hero and Gallery, section-by-section animations, and full design token treatment on all 7 sections (Hero, About, Timeline, Gallery, Locations, Contacts, RSVP).

Output: Visually premium public wedding page with parallax, animations, and design tokens throughout.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-redesign/06-RESEARCH.md

<interfaces>
From framer-motion (v12.34.3):
```typescript
import { motion, useScroll, useTransform, useReducedMotion } from 'framer-motion';
```

From src/components/ui/index.ts:
```typescript
export { Card } from './Card';
export { Input, type InputProps } from './Input';
export { Button, type ButtonProps } from './Button';
export { Badge, type BadgeProps } from './Badge';
```

Existing animation primitives:
```typescript
import { ScrollReveal } from '@/components/animation/ScrollReveal';
import { StaggerContainer } from '@/components/animation/StaggerContainer';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parallax to Hero and Gallery, polish all section components</name>
  <files>src/components/sections/Hero.tsx, src/components/sections/About.tsx, src/components/sections/Gallery.tsx, src/components/sections/Timeline.tsx, src/components/sections/Locations.tsx, src/components/sections/Contacts.tsx, src/components/sections/RSVP.tsx</files>
  <action>
Read all 7 section component files, then apply the following upgrades:

**Hero.tsx -- Parallax background:**
1. Import `{ useRef }` from 'react' and `{ motion, useScroll, useTransform, useReducedMotion }` from 'framer-motion'.
2. Create a ref for the section container.
3. Use `useScroll({ target: ref, offset: ['start start', 'end start'] })` to track scroll progress.
4. Create `backgroundY = useTransform(scrollYProgress, [0, 1], ['0%', '40%'])`.
5. Check `useReducedMotion()` -- if true, set backgroundY to static '0%'.
6. Check for mobile: use `useState` + `useEffect` with `window.matchMedia('(max-width: 768px)')` -- disable parallax on mobile (performance per RESEARCH.md Pitfall 1).
7. Structure:
   ```tsx
   <section ref={ref} className="relative min-h-screen overflow-hidden">
     <motion.div
       className="absolute inset-0 bg-cover bg-center"
       style={{ y: isMobile || prefersReducedMotion ? 0 : backgroundY, backgroundImage: '...' }}
     />
     <div className="relative z-10">
       {/* existing hero content */}
     </div>
   </section>
   ```
8. Apply design tokens: headings use `font-heading` (Cormorant Garamond), body text uses design token colors.
9. Replace any CSS `animate-*` keyframe classes with Framer Motion `initial`/`animate` for consistency.

**Gallery.tsx -- Parallax + hover effects:**
1. Same parallax pattern as Hero but with lighter intensity: `['0%', '20%']`.
2. Add hover effect on gallery items: wrap each image/placeholder in a div with `group` class, then `group-hover:scale-105 transition-transform duration-500` on the inner element, with `overflow-hidden` on the container.
3. Wrap each gallery item in `ScrollReveal` with staggered delay (index * 0.1s).
4. Keep existing grid layout (`grid-cols-2 md:grid-cols-3`).
5. Mobile: disable parallax, keep hover effects.

**About.tsx, Timeline.tsx, Locations.tsx, Contacts.tsx -- Design token treatment:**
For each of these 4 components:
1. Replace any hardcoded colors with CSS variable references (`var(--color-primary)`, `var(--color-text)`, etc.).
2. Ensure headings use `font-heading` class for Cormorant Garamond.
3. Ensure body text uses `text-[var(--color-text)]` and secondary text uses `text-[var(--color-text-light)]`.
4. Wrap in `ScrollReveal` if not already (most should already have it from Phase 3).
5. Add `Card` wrappers where appropriate for content blocks (timeline entries, location cards).
6. Ensure section backgrounds alternate: use `bg-[var(--color-background)]` or `bg-[var(--color-secondary)]` for visual rhythm.

**RSVP.tsx -- Card wrapper + styled inputs:**
1. Wrap the entire RSVP form in a `Card` component with `<Card.Header>` for the title and `<Card.Body>` for the form.
2. Verify radio buttons for attendance (Yes/No) are styled with design tokens. If using raw radio inputs, add custom styling:
   - Custom radio indicator: hidden native radio + styled div with `border-2 border-[var(--color-border)]` that fills with `bg-[var(--color-primary)]` when selected.
3. Ensure all form inputs already use `Input`, `Textarea` primitives (migrated in Phase 2). If any raw elements remain, replace them.
4. Keep the form as inline section (not modal -- per RESEARCH.md recommendation, confirmed in Claude's Discretion).
5. Keep all form validation logic (react-hook-form + zod) unchanged.

All components must keep `'use client'` directive. Do NOT change any data fetching, API calls, or form submission logic.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -q "useScroll" src/components/sections/Hero.tsx && grep -q "useScroll" src/components/sections/Gallery.tsx && grep -q "Card" src/components/sections/RSVP.tsx && grep -q "font-heading" src/components/sections/About.tsx && echo "PASS: Parallax on Hero/Gallery, Card on RSVP, design tokens on About" || echo "FAIL"</automated>
  </verify>
  <done>Hero and Gallery have parallax scroll effects (desktop only, reduced-motion aware). All 7 sections use OKLCH design tokens and Cormorant Garamond headings. RSVP wrapped in Card with styled form elements. Gallery has hover scale effects with staggered entrance.</done>
</task>

<task type="auto">
  <name>Task 2: Update Navigation component and wire couple names in /w/[slug]</name>
  <files>src/components/ui/Navigation.tsx, src/app/(public)/w/[slug]/page.tsx</files>
  <action>
**Navigation.tsx:**
1. Read the current Navigation component. It has a hardcoded "A & T" as the logo/wordmark.
2. Add a prop for couple names: `interface NavigationProps { partner1?: string; partner2?: string; }`.
3. Display couple names dynamically: `{partner1 || 'Partner 1'} & {partner2 || 'Partner 2'}` instead of hardcoded "A & T".
4. Apply design tokens:
   - Background: transparent initially, `bg-white/95 backdrop-blur-sm` on scroll (if scroll-aware behavior exists; if not, use solid `bg-white/95 backdrop-blur-sm`).
   - Text: `text-[var(--color-text)]` for nav items.
   - Wordmark: `font-heading text-xl` for Cormorant Garamond.
   - Borders: `border-b border-[var(--color-border)]`.
5. Keep existing nav link functionality and mobile behavior.

**w/[slug]/page.tsx:**
1. Read the current page component.
2. Pass couple names to the Navigation component: `<Navigation partner1={couple.partner1} partner2={couple.partner2} />`.
3. If the page fetches couple data from Supabase (it should), extract the partner names from the query result.
4. Keep the existing Footer.tsx for the wedding page (NOT SaasFooter -- the wedding page uses the couple-specific footer).
5. Do NOT change the data fetching logic or section component imports.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -q "partner1" src/components/ui/Navigation.tsx && grep -q "partner2" src/components/ui/Navigation.tsx && echo "PASS: Navigation accepts couple name props" || echo "FAIL"</automated>
  </verify>
  <done>Navigation component accepts partner1/partner2 props and displays them dynamically. /w/[slug] page passes couple names to Navigation. Design tokens applied to Navigation styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of public wedding page and dashboard</name>
  <files>none</files>
  <action>
Visual checkpoint -- no code changes. User verifies the complete UI redesign across all surfaces.

What was built:
1. Auth pages (login/register) with entrance animations
2. Dashboard navigation (desktop top bar + mobile bottom tab bar)
3. Dashboard views (checklist, budget, guests, chat, settings) with design primitives
4. Public wedding page with parallax scroll, design tokens, and premium polish
5. Footer with legal links, social, and contact
6. Three new pages: /tos, /privacy, /contact

How to verify -- run `npm run dev` and check:

Auth pages:
1. Visit http://localhost:3000/login -- card should fade in with upward slide
2. Visit http://localhost:3000/register -- same animation

Dashboard (login first or use demo mode):
3. Check desktop nav (>768px) -- polished top bar with all nav items
4. Check mobile nav (<768px) -- fixed bottom tab bar, no hamburger
5. Visit /checklist, /budget, /guests, /chat, /settings -- each should use Card wrappers, Button/Input primitives, design tokens
6. Settings page: all form fields should use UI primitive components

Public wedding page:
7. Visit http://localhost:3000/w/[any-slug] -- Hero section should have parallax on desktop
8. Scroll through all sections -- design tokens visible (warm colors, serif headings)
9. Gallery should have hover scale effects
10. RSVP form should be wrapped in a Card
11. Check on mobile viewport -- parallax should be disabled

Footer + new pages:
12. Landing page footer should show 4-column layout
13. Click TOS link -- /tos page with Czech content
14. Click Privacy link -- /privacy page with Czech content
15. Click Contact link -- /contact page with form
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>All UI redesign surfaces verified visually by user. Phase 6 complete.</done>
</task>

</tasks>

<verification>
1. Hero parallax: scroll the public wedding page on desktop -- background should move at ~40% scroll speed.
2. Gallery parallax: gallery section shows lighter parallax effect.
3. Reduced motion: set prefers-reduced-motion in browser dev tools -- all parallax should stop.
4. Mobile: resize to < 768px -- parallax disabled, all content still visible and scrollable.
5. Navigation: /w/[slug] page shows couple names in nav wordmark.
6. Design tokens: inspect any element -- colors should use oklch() values from CSS variables.
</verification>

<success_criteria>
- Hero and Gallery have parallax scroll effects on desktop
- Parallax disabled on mobile and for prefers-reduced-motion
- All 7 sections use OKLCH tokens and Cormorant Garamond headings
- RSVP form in Card wrapper
- Navigation shows dynamic couple names
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-redesign/06-04-SUMMARY.md`
</output>
