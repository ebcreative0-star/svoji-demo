---
phase: 06-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - src/components/dashboard/ChecklistView.tsx
  - src/components/dashboard/BudgetView.tsx
  - src/components/dashboard/GuestsView.tsx
  - src/components/dashboard/ChatInterface.tsx
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "All dashboard views use design system primitives (Button, Card, Input, Select, Textarea, Badge) instead of raw HTML elements"
    - "Settings page uses Input, Select, Textarea, and Button from the UI primitives"
    - "Dashboard views use consistent Card wrappers for content sections"
    - "Each dashboard view has a subtle entrance animation on mount"
    - "All dashboard views use OKLCH color tokens for text, borders, and backgrounds"
  artifacts:
    - path: "src/components/dashboard/ChecklistView.tsx"
      provides: "Checklist view with design system primitives"
      contains: "from '@/components/ui'"
    - path: "src/components/dashboard/BudgetView.tsx"
      provides: "Budget view with design system primitives"
      contains: "from '@/components/ui'"
    - path: "src/components/dashboard/GuestsView.tsx"
      provides: "Guest view with design system primitives"
      contains: "from '@/components/ui'"
    - path: "src/components/dashboard/ChatInterface.tsx"
      provides: "Chat interface with design system styling"
      contains: "var(--color-"
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page with full primitive substitution"
      contains: "from '@/components/ui'"
  key_links:
    - from: "src/components/dashboard/ChecklistView.tsx"
      to: "src/components/ui/index.ts"
      via: "import primitives"
      pattern: "import.*from '@/components/ui'"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/components/ui/index.ts"
      via: "import primitives"
      pattern: "import.*from '@/components/ui'"
---

<objective>
Systematically substitute raw HTML form elements and ad-hoc styling with design system primitives across all dashboard views and the settings page.

Purpose: Dashboard views still use raw `<input>`, `<button>`, `<select>`, `<textarea>` with ad-hoc Tailwind classes. Phase 2 created the typed UI primitives but only migrated some views. This plan completes the migration for ALL dashboard surfaces, making the app visually consistent with the design system.

Output: All 5 dashboard surfaces using UI primitives with consistent design tokens and subtle entrance animations.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-redesign/06-RESEARCH.md

<interfaces>
From src/components/ui/index.ts:
```typescript
export { Button, type ButtonProps } from './Button';
export { Card } from './Card';
export { Input, type InputProps } from './Input';
export { Textarea, type TextareaProps } from './Textarea';
export { Select, type SelectProps } from './Select';
export { Badge, type BadgeProps } from './Badge';
export { FormInput } from './FormInput';
export { FormTextarea } from './FormTextarea';
export { FormSelect } from './FormSelect';
```

Card sub-components (dot notation):
```typescript
<Card>
  <Card.Header>Title</Card.Header>
  <Card.Body>Content</Card.Body>
  <Card.Footer>Actions</Card.Footer>
</Card>
```

Button variants: primary, secondary, ghost, danger
Button sizes: sm, md, lg
Badge intents: neutral, success, warning, danger, info
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ChecklistView, BudgetView, and GuestsView to design system primitives</name>
  <files>src/components/dashboard/ChecklistView.tsx, src/components/dashboard/BudgetView.tsx, src/components/dashboard/GuestsView.tsx</files>
  <action>
For each of the three dashboard view files, read the current code and apply these changes:

**General pattern for all three:**
1. Import `{ Button, Card, Input, Select, Badge }` from `'@/components/ui'` (only what's needed per file).
2. Import `{ motion }` from `'framer-motion'`.
3. Wrap the top-level container in a `motion.div` with `initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.2 }}` for subtle mount fade.
4. Replace raw `<input>` elements with `<Input>` component, passing `label`, `value`, `onChange` props.
5. Replace raw `<button>` elements with `<Button>` component, using appropriate `variant` (primary for main actions, secondary for alternatives, ghost for subtle, danger for delete).
6. Replace raw `<select>` elements with `<Select>` component.
7. Wrap content sections in `<Card>` with `<Card.Header>` for section titles and `<Card.Body>` for content.
8. Replace hardcoded color values with CSS variable references: `text-[var(--color-primary)]`, `bg-[var(--color-secondary)]`, etc.
9. Keep all existing business logic, state management, and data flow unchanged. This is visual substitution only.

**ChecklistView specific:**
- Phase 2 already migrated filter buttons and badges. Focus on: any remaining raw inputs (add item form), any raw buttons not yet migrated, section headings, and overall Card wrapper.
- Keep the custom circle toggle button as-is (it has a complex custom shape per Phase 2 decision).
- Wrap the overall checklist in a Card component.

**BudgetView specific:**
- Phase 2 already migrated some parts. Focus on: the add-item form inputs and buttons, the category section headers, any remaining raw elements.
- Use `Card` for the budget summary section at the top.
- Replace any raw `<input type="number">` with `<Input type="number">`.

**GuestsView specific:**
- Phase 2 already migrated some parts. Focus on: the add-guest form inputs, any remaining raw buttons, the guest list table/grid styling.
- Use `Card` wrapper for the guest list section.
- Replace any raw `<input>` in the add-guest form with `<Input>`.
- Ensure the RSVP status badges (already migrated in Phase 2) still work.

Do NOT change any Supabase queries, state management patterns, or business logic. Pure visual layer changes.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -c "from '@/components/ui'" src/components/dashboard/ChecklistView.tsx src/components/dashboard/BudgetView.tsx src/components/dashboard/GuestsView.tsx | grep -v ":0" | wc -l | grep -q "3" && echo "PASS: All three views import UI primitives" || echo "FAIL"</automated>
  </verify>
  <done>ChecklistView, BudgetView, and GuestsView use Card wrappers, Button/Input/Select primitives, OKLCH tokens, and subtle entrance animations. No raw HTML form elements remain except the ChecklistView circle toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ChatInterface and Settings page to design system primitives</name>
  <files>src/components/dashboard/ChatInterface.tsx, src/app/(dashboard)/settings/page.tsx</files>
  <action>
**ChatInterface.tsx:**
Read the current ChatInterface code and apply:
1. Import `{ Button, Card, Input }` from `'@/components/ui'` as needed.
2. Import `{ motion }` from `'framer-motion'` for mount animation.
3. Wrap the top-level container in `motion.div` with subtle fade-in.
4. Replace the message input (if raw `<input>`) with `<Input>` component. The chat input may need custom styling to fit the chat UI pattern -- if so, keep the visual styling but use design tokens (OKLCH variables).
5. Replace the send button (if raw `<button>`) with `<Button variant="primary" size="sm">`.
6. Apply design tokens to:
   - Message bubbles: user messages use `bg-[var(--color-primary)] text-white`, AI messages use `bg-[var(--color-secondary)]`.
   - Chat container: `border border-[var(--color-border)]` rounded corners.
   - Timestamp text: `text-[var(--color-text-light)]`.
7. Keep the Loader2 spinner for AI thinking indicator (Phase 2 decision).
8. Keep all chat logic, message sending, streaming, etc. unchanged.

**Settings page (src/app/(dashboard)/settings/page.tsx):**
This is the biggest substitution target. Read the current code and apply:
1. Add `'use client'` if not present.
2. Import `{ Button, Card, Input, Select, Textarea }` from `'@/components/ui'`.
3. Import `{ motion }` from `'framer-motion'` for mount animation.
4. Wrap the page in `motion.div` with fade-in.
5. Replace ALL raw `<input>` with `<Input>` component:
   - Pass `label` prop for field labels (currently labels may be separate `<label>` elements).
   - Pass `value`, `onChange`, `type` props.
6. Replace ALL raw `<select>` with `<Select>` component:
   - Keep `<option>` children as-is (Select uses children approach).
7. Replace ALL raw `<textarea>` with `<Textarea>` component.
8. Replace ALL raw `<button>` with `<Button>` component:
   - Save/submit buttons: `variant="primary"`.
   - Cancel buttons: `variant="secondary"`.
   - Delete/dangerous buttons: `variant="danger"`.
9. Wrap each settings section (profile, wedding details, etc.) in `<Card>` with `<Card.Header>` for section title and `<Card.Body>` for fields.
10. Apply design tokens to all text colors, backgrounds, borders.
11. Keep all existing save logic, Supabase updates, state management unchanged.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -q "from '@/components/ui'" src/app/\(dashboard\)/settings/page.tsx && grep -q "var(--color-" src/components/dashboard/ChatInterface.tsx && echo "PASS: Settings uses UI primitives, Chat uses design tokens" || echo "FAIL"</automated>
  </verify>
  <done>ChatInterface uses design tokens for message bubbles, input, and send button. Settings page uses Card sections with Input/Select/Textarea/Button primitives for all form fields. No raw HTML form elements remain in the settings page.</done>
</task>

</tasks>

<verification>
1. Visit /checklist -- content wrapped in Cards, buttons use Button component, design tokens visible.
2. Visit /budget -- budget summary in Card, add-item form uses Input/Button primitives.
3. Visit /guests -- guest list in Card, add-guest form uses Input primitives.
4. Visit /chat -- message bubbles use design tokens, input area uses design tokens.
5. Visit /settings -- all form fields use Input/Select/Textarea, save button uses Button, sections wrapped in Cards.
6. All views show subtle fade-in on mount.
</verification>

<success_criteria>
- Zero raw `<input>`, `<select>`, `<textarea>` elements in settings page
- All dashboard views import from '@/components/ui'
- All views use OKLCH color tokens (no hardcoded colors)
- Each view has motion.div entrance animation
- Business logic unchanged -- all CRUD operations still work
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-redesign/06-03-SUMMARY.md`
</output>
