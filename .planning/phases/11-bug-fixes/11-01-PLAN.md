---
phase: 11-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/DashboardNav.tsx
  - src/components/dashboard/ChecklistView.tsx
autonomous: true
requirements:
  - BUG-01
  - BUG-04

must_haves:
  truths:
    - "User sees partner names in the mobile top bar after login"
    - "Checklist header shows exactly 3 stat cards: Hotovo, Zbývá, Po termínu"
    - "Zbývá card displays days-to-wedding number as its main value"
    - "Progres stat card is removed entirely"
  artifacts:
    - path: "src/components/dashboard/DashboardNav.tsx"
      provides: "Partner names in mobile nav bar"
      contains: "partner1.*partner2"
    - path: "src/components/dashboard/ChecklistView.tsx"
      provides: "3-card stat grid with countdown"
      contains: "grid-cols-3"
  key_links:
    - from: "src/components/dashboard/DashboardNav.tsx"
      to: "src/app/(dashboard)/layout.tsx"
      via: "partner1 and partner2 props passed from layout"
      pattern: "partner1.*partner2"
    - from: "src/components/dashboard/ChecklistView.tsx"
      to: "date-fns differenceInDays"
      via: "daysUntilWedding calculation already exists"
      pattern: "differenceInDays"
---

<objective>
Fix dashboard mobile partner names (BUG-01) and checklist countdown display (BUG-04).

Purpose: Two pure-UI fixes that restore trust on first use -- users see their names and a clear wedding countdown instead of confusing stats.
Output: Updated DashboardNav.tsx and ChecklistView.tsx
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bug-fixes/11-CONTEXT.md
@.planning/phases/11-bug-fixes/11-RESEARCH.md
@src/components/dashboard/DashboardNav.tsx
@src/components/dashboard/ChecklistView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add partner names to mobile nav bar (BUG-01)</name>
  <files>src/components/dashboard/DashboardNav.tsx</files>
  <action>
In DashboardNav.tsx, find the mobile top bar `<nav>` element (the one with `md:hidden`). Inside the `<Link>` that shows the "Svoji" logo text, add a `<span>` after the logo span that displays `{partner1} & {partner2}`.

Style it: `text-xs text-[var(--color-text-light)]` to be subtle next to the logo.

The `partner1` and `partner2` props are already received by this component from the dashboard layout -- they just aren't rendered in the mobile nav. No data fetching changes needed.

If either partner name is empty/undefined, don't render the span at all (guard with `{partner1 && partner2 && (...)}`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Mobile nav bar shows "Svoji partner1 & partner2" text. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix checklist stat cards to show wedding countdown (BUG-04)</name>
  <files>src/components/dashboard/ChecklistView.tsx</files>
  <action>
In ChecklistView.tsx, find the stat card grid (around line 139). Make these exact changes:

1. Change the grid class from `grid-cols-2 lg:grid-cols-4` to `grid-cols-3`. Keep the existing gap classes.

2. Remove the "Progres" StatCard entirely (the 4th card showing percentage).

3. Change the "Zbývá" StatCard:
   - Change `value` from `stats.pending.toString()` to a days-to-wedding string
   - Change `subtitle` from the days string to `"do svatby"`
   - Handle negative days gracefully for past weddings

The `daysUntilWedding` variable is already calculated earlier in the component via `differenceInDays`.

Exact value logic:
```tsx
const daysLabel = daysUntilWedding >= 0
  ? `${daysUntilWedding} dní`
  : 'Proběhla'
```

Use `daysLabel` as the StatCard value, with subtitle `"do svatby"` (only show subtitle when `daysUntilWedding >= 0`).

Result: 3 cards -- Hotovo (X/Y), Zbývá (180 dní / do svatby), Po termínu -- stretching full width.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Checklist header shows exactly 3 stat cards in a single row. Zbývá card shows days-to-wedding as main value. Progres card is gone. Past wedding dates show "Proběhla" instead of negative days.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Visual check: mobile viewport shows partner names in top nav bar
- Visual check: checklist page shows 3 evenly-spaced stat cards with days countdown
</verification>

<success_criteria>
- Partner names visible on mobile nav (not just desktop)
- Checklist shows 3 cards: Hotovo, Zbývá (days countdown), Po termínu
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/11-bug-fixes/11-01-SUMMARY.md`
</output>
