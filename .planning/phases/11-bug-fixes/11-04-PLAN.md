---
phase: 11-bug-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - BUG-03
gap_closure: true

must_haves:
  truths:
    - "budget_items table has a source column in production Supabase"
    - "AI chat can successfully add budget items"
    - "AI-created budget items show sparkles badge in BudgetView"
  artifacts:
    - path: "supabase/migrations/008_budget_item_source.sql"
      provides: "Migration adding source column"
      contains: "ADD COLUMN.*source"
  key_links:
    - from: "src/lib/ai/action-executor.ts"
      to: "budget_items.source column"
      via: "insert with source: 'ai'"
      pattern: "source.*ai"
---

<objective>
Apply migration 008_budget_item_source.sql to production Supabase so AI budget item insertion works.

Purpose: The migration file exists locally but was never applied to production. The source column doesn't exist in the production budget_items table, causing every AI budget insert to fail with PostgreSQL error 42703. The code is correct -- only the DB schema is missing.
Output: Production database has the source column, AI budget items work.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-bug-fixes/11-02-SUMMARY.md
@.planning/debug/ai-chat-budget-add-fails.md
@supabase/migrations/008_budget_item_source.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 008 to production Supabase</name>
  <action>
The migration file `supabase/migrations/008_budget_item_source.sql` already exists with the correct SQL. It just needs to be applied to the production Supabase database.

Run one of these:

**Option A -- Supabase CLI:**
```bash
supabase db push
```

**Option B -- SQL Editor (Supabase Dashboard):**
Go to your Supabase project dashboard -> SQL Editor -> New Query, paste:
```sql
ALTER TABLE budget_items
  ADD COLUMN IF NOT EXISTS source VARCHAR(20) DEFAULT 'manual';

UPDATE budget_items SET source = 'manual' WHERE source IS NULL;
```
Run the query.

**Option C -- Direct psql:**
```bash
psql "$SUPABASE_DB_URL" -f supabase/migrations/008_budget_item_source.sql
```
  </action>
  <verify>User confirms migration applied successfully</verify>
  <done>Source column exists on budget_items table in production</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AI budget items work and show sparkles badge</name>
  <action>
Migration 008 has been applied. AI budget items should now work and show sparkles badge.

Verification steps:
1. Open the app and go to the AI chat
2. Type: "pridej catering za 30000 do rozpoctu"
3. AI should confirm the item was added
4. Navigate to the Budget page
5. The "catering" item should appear with a small sparkles icon next to its name
6. Verify you can edit and delete the AI-created item just like a manual item
  </action>
  <verify>User confirms AI budget items work with sparkles badge</verify>
  <done>AI can add budget items, sparkles badge is visible, items are editable and deletable</done>
</task>

</tasks>

<verification>
- Migration 008 applied to production (source column exists on budget_items)
- AI can add budget items without error
- Sparkles badge visible on AI-created items
</verification>

<success_criteria>
- AI chat successfully adds budget items to the database
- AI-created items show sparkles icon in BudgetView
- AI items are fully editable and deletable
</success_criteria>

<output>
After completion, create `.planning/phases/11-bug-fixes/11-04-SUMMARY.md`
</output>
