---
phase: 11.1-ai-actions-batch-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/intent-classifier.ts
  - src/lib/ai/action-executor.ts
  - src/components/dashboard/BudgetView.tsx
autonomous: true
requirements: [BUG-05, BUG-06, BUG-07]

must_haves:
  truths:
    - "User says 'pridej catering 50000 do rozpoctu' and the item appears in budget"
    - "User says 'smaz dort z rozpoctu' and the item is removed"
    - "All budget items in DB are visible in BudgetView, no phantom totals"
  artifacts:
    - path: "src/lib/ai/intent-classifier.ts"
      provides: "Improved classifier prompt with budget_add examples requiring amount, budget_remove examples"
      contains: "budget_add"
    - path: "src/lib/ai/action-executor.ts"
      provides: "Category normalization in addBudgetItem, fix falsy amount check"
      contains: "addBudgetItem"
    - path: "src/components/dashboard/BudgetView.tsx"
      provides: "Catch-all 'other' fallback for uncategorized items"
      contains: "BUDGET_CATEGORIES"
  key_links:
    - from: "src/lib/ai/intent-classifier.ts"
      to: "src/lib/ai/action-executor.ts"
      via: "intent classification result"
      pattern: "budget_add.*confidence"
    - from: "src/lib/ai/action-executor.ts"
      to: "src/components/dashboard/BudgetView.tsx"
      via: "budget_items DB table with normalized category"
      pattern: "category.*BUDGET_CATEGORIES"
---

<objective>
Fix three AI/budget bugs: classifier not triggering budget_add (BUG-05), budget_remove not working via chat (BUG-06), and phantom budget items invisible in UI (BUG-07).

Purpose: Users trust the AI to actually execute actions when asked, and see all their budget data.
Output: Reliable budget add/remove via chat, no phantom items in budget view.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-ai-actions-batch-import/11.1-RESEARCH.md

@src/lib/ai/intent-classifier.ts
@src/lib/ai/action-executor.ts
@src/components/dashboard/BudgetView.tsx
@src/app/api/chat/route.ts

<interfaces>
<!-- Key types the executor needs -->

From src/lib/ai/intent-classifier.ts:
```typescript
export interface IntentResult {
  intent: string;
  confidence: number;
  params: Record<string, any>;
}
export async function classifyIntent(message: string, conversationContext?: string[]): Promise<IntentResult>;
export function isActionIntent(intent: string): boolean;
```

From src/lib/ai/action-executor.ts:
```typescript
export interface ActionResult {
  success: boolean;
  message: string;
  data?: any;
}
export async function executeAction(supabase, coupleId, intent, params): Promise<ActionResult>;
```

From src/components/dashboard/BudgetView.tsx:
```typescript
const BUDGET_CATEGORIES = [
  { value: 'venue', label: 'Misto', icon: '...' },
  { value: 'catering', label: 'Catering', icon: '...' },
  // ... 10 more including 'other'
];
// groupedItems filters by item.category === cat.value -- silently drops unknown categories
// totalEstimated sums ALL items unfiltered -- causes phantom total
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix intent classifier for budget actions (BUG-05, BUG-06)</name>
  <files>src/lib/ai/intent-classifier.ts</files>
  <action>
Two bugs in the classifier prompt need fixing:

**BUG-05 (budget_add not triggering):**
1. Add more Czech-language examples for budget_add to CLASSIFICATION_SYSTEM_PROMPT. Currently there are examples like "Mame 50000 na catering" but common phrasings like "pridej fotograf 30000 do rozpoctu" or "rozpocet: fotograf 25000" are missing.
2. Add a new PRAVIDLA rule: "Pokud uzivatel zminuje castku A nazev polozky, VZDY klasifikuj jako budget_add s params { name, amount, category }. Bez castky klasifikuj jako advice_request."
3. Add at least 4 new budget_add examples covering common phrasings:
   - "Pridej fotografa za 25000" -> budget_add, confidence 0.95, params {name: "fotograf", amount: 25000, category: "photo"}
   - "Rozpocet na kvetiny 8000" -> budget_add, confidence 0.95, params {name: "kvetiny", amount: 8000, category: "flowers"}
   - "Catering bude stat 80000" -> budget_add, confidence 0.95, params {name: "catering", amount: 80000, category: "catering"}
   - "Pridej do rozpoctu DJ 15000" -> budget_add, confidence 0.95, params {name: "DJ", amount: 15000, category: "music"}
4. Add a category mapping hint to the prompt: "Pro budget_add category pouzij: venue|catering|photo|music|flowers|attire|rings|decor|cake|transport|honeymoon|other"

**BUG-06 (budget_remove not working):**
1. The budget_remove intent and action-executor handler BOTH exist. The issue is likely low confidence classification. Add more examples:
   - "Smaz kvetiny z rozpoctu" -> budget_remove, confidence 0.95, params {name: "kvetiny"}
   - "Zrus polozku catering" -> budget_remove, confidence 0.95, params {name: "catering"}
   - "Nechci uz dort v rozpoctu" -> budget_remove, confidence 0.95, params {name: "dort"}
2. Add PRAVIDLA rule: "Pokud uzivatel chce neco smazat/zrusit/odstranit z rozpoctu, klasifikuj jako budget_remove."
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -c "budget_add" src/lib/ai/intent-classifier.ts | awk '{if ($1 > 10) print "PASS: budget_add examples added"; else print "FAIL: not enough budget_add examples"}'</automated>
  </verify>
  <done>Classifier prompt has sufficient budget_add and budget_remove examples and rules. At least 4 new budget_add examples and 3 new budget_remove examples added. Category mapping hint included in prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Fix phantom budget items and amount validation (BUG-07)</name>
  <files>src/lib/ai/action-executor.ts, src/components/dashboard/BudgetView.tsx</files>
  <action>
**In action-executor.ts `addBudgetItem` function:**
1. Fix the falsy amount check: change `if (!name || !amount)` to `if (!name || amount == null || amount === undefined)`. The current check treats amount=0 as missing.
2. Add category normalization before insert. Create a `normalizeBudgetCategory` helper that maps common Czech/alternate terms to BUDGET_CATEGORIES values:
   ```
   const CATEGORY_MAP: Record<string, string> = {
     'fotograf': 'photo', 'foto': 'photo', 'video': 'photo', 'fotografie': 'photo',
     'hudba': 'music', 'dj': 'music', 'kapela': 'music', 'muzika': 'music',
     'jidlo': 'catering', 'stravovani': 'catering', 'menu': 'catering',
     'kvetiny': 'flowers', 'kytky': 'flowers', 'floristka': 'flowers',
     'misto': 'venue', 'salonek': 'venue', 'zamek': 'venue',
     'saty': 'attire', 'oblek': 'attire', 'obleceni': 'attire',
     'prsteny': 'rings', 'snubni': 'rings', 'prsten': 'rings',
     'dekorace': 'decor', 'vyzdoba': 'decor',
     'dort': 'cake',
     'doprava': 'transport', 'auto': 'transport', 'limuzina': 'transport',
     'libanky': 'honeymoon', 'svatebni_cesta': 'honeymoon',
   };
   ```
   If the category from params doesn't match any BUDGET_CATEGORIES value, look it up in CATEGORY_MAP (lowercase). If still no match, default to 'other'.
3. Apply this normalization in both `addBudgetItem` before the insert call.

**In BudgetView.tsx:**
1. After the existing `groupedItems` computation (which maps BUDGET_CATEGORIES and filters items), add a catch-all: collect any items whose `item.category` does NOT appear in BUDGET_CATEGORIES values, and if there are any, push them into the 'other' group. This ensures no items are silently dropped from the UI.
2. The 'other' category already exists in BUDGET_CATEGORIES. So find the 'other' group in `groupedItems` and merge uncategorized items into it.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -q "normalizeBudgetCategory\|CATEGORY_MAP" src/lib/ai/action-executor.ts && grep -q "other" src/components/dashboard/BudgetView.tsx && echo "PASS: category normalization and catch-all added" || echo "FAIL"</automated>
  </verify>
  <done>Budget items with any category value are visible in BudgetView (no phantom items). Amount=0 is accepted. AI-generated categories are normalized to known BUDGET_CATEGORIES values before DB insert. Items with truly unknown categories fall into "Ostatni" group.</done>
</task>

</tasks>

<verification>
1. Classifier prompt contains at least 4 new budget_add examples with category hints
2. Classifier prompt contains at least 3 new budget_remove examples
3. action-executor.ts has normalizeBudgetCategory function mapping Czech terms to BUDGET_CATEGORIES
4. BudgetView.tsx has catch-all for uncategorized items merged into 'other' group
5. Amount validation uses `== null` not `!amount`
</verification>

<success_criteria>
- AI intent pipeline reliably classifies budget add/remove requests with high confidence
- Budget items are always inserted with a valid BUDGET_CATEGORIES value
- BudgetView renders ALL items from DB, zero phantom totals
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-ai-actions-batch-import/11.1-01-SUMMARY.md`
</output>
