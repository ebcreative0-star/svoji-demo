---
phase: 03-animation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/providers/LenisProvider.tsx
  - src/components/animation/ScrollReveal.tsx
  - src/components/animation/StaggerContainer.tsx
  - src/components/animation/FrozenRouter.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-05]

must_haves:
  truths:
    - "Lenis smooth scroll is active on all pages -- mouse wheel scrolling feels physically smooth with moderate lerp"
    - "Lenis is disabled when prefers-reduced-motion is enabled -- native scroll resumes"
    - "Lenis does not intercept touch scroll events on mobile (syncTouch: false)"
    - "MotionConfig reducedMotion='user' wraps entire app -- all motion.* children respect OS reduced-motion setting"
    - "ScrollReveal component fades and slides children up 24px when scrolled into view, fires once"
    - "StaggerContainer staggers child ScrollReveal items at ~100ms intervals"
    - "FrozenRouter holds previous LayoutRouterContext during route transitions to prevent premature unmounting"
  artifacts:
    - path: "src/components/providers/LenisProvider.tsx"
      provides: "Lenis smooth scroll root wrapper with reduced-motion bypass"
      exports: ["LenisProvider"]
    - path: "src/components/animation/ScrollReveal.tsx"
      provides: "Scroll-triggered fade+slide-up reveal wrapper"
      exports: ["ScrollReveal"]
    - path: "src/components/animation/StaggerContainer.tsx"
      provides: "Parent container that staggers child whileInView animations"
      exports: ["StaggerContainer"]
    - path: "src/components/animation/FrozenRouter.tsx"
      provides: "App Router context freeze for AnimatePresence exit animations"
      exports: ["FrozenRouter"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with MotionConfig + LenisProvider wrapping"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/LenisProvider.tsx"
      via: "LenisProvider wrapping children in body"
      pattern: "import.*LenisProvider"
    - from: "src/app/layout.tsx"
      to: "framer-motion MotionConfig"
      via: "MotionConfig reducedMotion='user' wrapping LenisProvider"
      pattern: "MotionConfig.*reducedMotion"
    - from: "src/components/providers/LenisProvider.tsx"
      to: "lenis/react"
      via: "ReactLenis root component"
      pattern: "import.*ReactLenis.*lenis/react"
    - from: "src/components/animation/FrozenRouter.tsx"
      to: "next/dist/shared/lib/app-router-context.shared-runtime"
      via: "Internal Next.js LayoutRouterContext for freeze pattern"
      pattern: "LayoutRouterContext"
---

<objective>
Install Lenis and create the animation infrastructure layer: LenisProvider for smooth scroll, MotionConfig for global reduced-motion support, ScrollReveal/StaggerContainer for scroll-triggered reveals, and FrozenRouter for page transition support.

Purpose: Build reusable animation primitives that Phase 3 Plan 02 wires into existing components and routes, and that phases 4-7 consume for all surface animations.
Output: 4 new component files, 1 modified layout, lenis installed.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-animation-layer/03-RESEARCH.md

<interfaces>
<!-- Existing root layout structure the executor needs to modify -->

From src/app/layout.tsx:
```typescript
// Current: plain body wrapping children
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="cs" className={`${inter.variable} ${cormorant.variable}`}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
```

From framer-motion (already installed v12.34.3):
```typescript
import { motion, MotionConfig, useReducedMotion, AnimatePresence } from 'framer-motion';
```

From lenis/react (to be installed):
```typescript
import { ReactLenis, useLenis } from 'lenis/react';
// ReactLenis props: root, options, children
// options: { lerp, duration, smoothWheel, syncTouch, ... }
```

From next/dist/shared/lib/app-router-context.shared-runtime (internal):
```typescript
import { LayoutRouterContext } from 'next/dist/shared/lib/app-router-context.shared-runtime';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Lenis and create LenisProvider + animation components</name>
  <files>
    package.json,
    src/components/providers/LenisProvider.tsx,
    src/components/animation/ScrollReveal.tsx,
    src/components/animation/StaggerContainer.tsx,
    src/components/animation/FrozenRouter.tsx
  </files>
  <action>
1. Run `npm install lenis` to add smooth scroll library.

2. Create `src/components/providers/LenisProvider.tsx`:
   - 'use client' directive
   - Import `ReactLenis` from `lenis/react` and `useReducedMotion` from `framer-motion`
   - Check `useReducedMotion()` -- if true, return `<>{children}</>` (skip Lenis entirely)
   - Otherwise render `<ReactLenis root options={{ lerp: 0.1, duration: 1.2, smoothWheel: true, syncTouch: false }}>` wrapping children
   - syncTouch: false per Claude's discretion -- avoids jank on older mobile devices while keeping desktop smooth
   - Export named `LenisProvider`

3. Create `src/components/animation/ScrollReveal.tsx`:
   - 'use client' directive
   - Import `motion` from `framer-motion`
   - Props: `children: ReactNode`, `delay?: number` (default 0), `className?: string`
   - Variants: hidden `{ opacity: 0, y: 24 }`, visible `{ opacity: 1, y: 0 }`
   - Render `<motion.div>` with `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, margin: '-80px' }}`, `variants`, `transition={{ duration: 0.45, ease: 'easeOut', delay }}`
   - Export named `ScrollReveal`

4. Create `src/components/animation/StaggerContainer.tsx`:
   - 'use client' directive
   - Import `motion` from `framer-motion`
   - Props: `children: ReactNode`, `className?: string`, `staggerDelay?: number` (default 0.1)
   - Container variants: hidden `{}`, visible `{ transition: { staggerChildren: staggerDelay } }`
   - Use a function to generate variants with the staggerDelay parameter
   - Render `<motion.div>` with `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, margin: '-60px' }}`, `variants={containerVariants(staggerDelay)}`
   - Export named `StaggerContainer`
   - NOTE: Children of StaggerContainer should use ScrollReveal or their own motion.div with matching variant names (hidden/visible) for stagger to propagate

5. Create `src/components/animation/FrozenRouter.tsx`:
   - 'use client' directive
   - Import `LayoutRouterContext` from `next/dist/shared/lib/app-router-context.shared-runtime`
   - Import `useSelectedLayoutSegment` from `next/navigation`
   - Import `useContext`, `useEffect`, `useRef` from `react`
   - Add comment: `// WARNING: Uses internal Next.js API (LayoutRouterContext). May break on Next.js major upgrades.`
   - Implement `usePreviousValue<T>` hook using useRef (stores previous value, updates in useEffect)
   - `FrozenRouter` component: captures `LayoutRouterContext` via `useContext`, tracks previous context and segment, detects segment change, provides previous context during transitions via `LayoutRouterContext.Provider`
   - Export named `FrozenRouter`
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && node -e "require('lenis')" && echo "lenis installed OK" && npx tsc --noEmit src/components/providers/LenisProvider.tsx src/components/animation/ScrollReveal.tsx src/components/animation/StaggerContainer.tsx src/components/animation/FrozenRouter.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - `lenis` appears in package.json dependencies
    - LenisProvider.tsx exports LenisProvider, uses ReactLenis with root + options, bypasses when reduced motion
    - ScrollReveal.tsx exports ScrollReveal with fade+slide-up (y: 24), once: true, margin: -80px
    - StaggerContainer.tsx exports StaggerContainer with staggerChildren propagation
    - FrozenRouter.tsx exports FrozenRouter with LayoutRouterContext freeze pattern
    - All files have 'use client' directive
    - All files compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire MotionConfig and LenisProvider into root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
1. Modify `src/app/layout.tsx`:
   - Add `'use client'` -- NO! layout.tsx must stay a Server Component for metadata exports. Instead:
   - Import `MotionConfig` from `framer-motion` -- this won't work in a Server Component.

   CORRECTION: layout.tsx is a Server Component (exports metadata). MotionConfig and LenisProvider are client components. The pattern is:
   - Create a client wrapper component OR use the providers inline since Next.js allows importing client components into server components (they just render on the client).
   - Actually, importing a client component (`LenisProvider`) into a server component (`layout.tsx`) works fine in Next.js App Router -- the client component renders on the client, the server component renders on the server. The boundary is at the `'use client'` directive in the imported file.

   Approach: Create a single `Providers.tsx` wrapper that combines MotionConfig + LenisProvider, then import into layout.tsx.

   a. Create `src/components/providers/Providers.tsx`:
      - 'use client' directive
      - Import `MotionConfig` from `framer-motion`
      - Import `LenisProvider` from `@/components/providers/LenisProvider`
      - Render: `<MotionConfig reducedMotion="user"><LenisProvider>{children}</LenisProvider></MotionConfig>`
      - Export named `Providers`

   b. Modify `src/app/layout.tsx`:
      - Add import: `import { Providers } from '@/components/providers/Providers'`
      - Wrap `{children}` with `<Providers>{children}</Providers>` inside body
      - Keep ALL existing code (metadata, fonts, viewport) unchanged
      - Final structure: `<html><body className="antialiased"><Providers>{children}</Providers></body></html>`
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && npx tsc --noEmit src/app/layout.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - layout.tsx remains a Server Component (no 'use client', metadata export intact)
    - Providers.tsx is 'use client', wraps MotionConfig + LenisProvider
    - layout.tsx imports and renders Providers wrapping children
    - MotionConfig reducedMotion="user" is the outermost wrapper inside Providers
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npm ls lenis` confirms lenis is installed
2. `npx tsc --noEmit` passes for all new and modified files
3. Grep confirms: `MotionConfig reducedMotion` in Providers.tsx, `ReactLenis root` in LenisProvider.tsx, `whileInView` in ScrollReveal.tsx, `staggerChildren` in StaggerContainer.tsx, `LayoutRouterContext` in FrozenRouter.tsx
4. layout.tsx still exports metadata (Server Component intact)
</verification>

<success_criteria>
- Lenis is installed and importable
- LenisProvider wraps the entire app via root layout
- MotionConfig reducedMotion="user" wraps the entire app
- ScrollReveal, StaggerContainer, and FrozenRouter components exist and compile
- Reduced motion path tested: LenisProvider returns bare children when useReducedMotion() is true
- All 5 new files are 'use client' components
- layout.tsx remains a Server Component
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-layer/03-01-SUMMARY.md`
</output>
