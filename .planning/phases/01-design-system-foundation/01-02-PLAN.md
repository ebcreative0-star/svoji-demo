---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04

must_haves:
  truths:
    - "All pages display warm-premium colors with no broken or missing color references"
    - "Headings render in Cormorant Garamond with no layout shift or fallback font flash"
    - "Czech diacritics in headings render correctly (no fallback font for individual characters)"
    - "Spacing is consistent across pages"
  artifacts: []
  key_links: []
---

<objective>
Visually verify the design system migration on the running dev server. Confirm colors, typography, and spacing render correctly across all major surfaces.

Purpose: Catch visual regressions that a build check cannot detect -- wrong colors, font loading issues, Czech character rendering, layout shifts.
Output: Human confirmation that the design system foundation is visually correct.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run automated token verification</name>
  <files></files>
  <action>
1. Start the Next.js dev server: `npm run dev`
2. Wait for it to compile successfully
3. Use curl to fetch the landing page HTML and verify:
   - The `<html>` tag contains both font CSS variable classes (from next/font)
   - The page renders without server errors (200 status)
4. Check that no references to `Playfair` remain in the codebase:
   ```bash
   grep -r "Playfair" src/ --include="*.tsx" --include="*.ts" --include="*.css"
   ```
   This should return zero results.
5. Check that no hex color values remain in globals.css `:root` or `@theme`:
   ```bash
   grep -n "#[0-9A-Fa-f]\{6\}" src/app/globals.css
   ```
   Should return zero results (hex values only allowed in comments, not in variable definitions).
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -r "Playfair" src/ --include="*.tsx" --include="*.ts" --include="*.css" | grep -v "// " | wc -l | tr -d ' '</automated>
  </verify>
  <done>Dev server starts cleanly, no Playfair references remain in source files, no hex color values in @theme block</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of design system</name>
  <files></files>
  <action>
Human visual verification of the design system migration.

What was built: Complete design token migration -- OKLCH warm-premium palette, Cormorant Garamond headings, spacing tokens via Tailwind 4 @theme.

How to verify:
1. Open http://localhost:3000 in your browser
2. Check these things on the landing page:
   - Headings use Cormorant Garamond (elegant serif, thinner than Playfair Display)
   - Body text uses Inter
   - Colors feel warm-premium (champagne/gold primary, not the old mocha brown)
   - No broken colors (nothing showing raw variable names or defaulting to black)
3. Check a page with Czech text:
   - Verify diacritics (c, r, z, e, s, u with hacky/carky) render in the heading font, not a fallback
4. Check the login page (/login) for consistent styling
5. If any color feels wrong or the typography doesn't look right, describe what you see

Resume signal: Type "approved" if everything looks good, or describe any issues.
  </action>
  <verify>
    <automated>echo "Human visual verification -- no automated check"</automated>
  </verify>
  <done>Human approves the visual appearance of colors, typography, and spacing across the application</done>
</task>

</tasks>

<verification>
Human confirms visual quality of the migrated design system on the running application.
</verification>

<success_criteria>
- Human approves visual appearance of colors, typography, and spacing
- No Playfair Display references remain
- Czech diacritics render correctly
- No visual regressions on any page
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-02-SUMMARY.md`
</output>
