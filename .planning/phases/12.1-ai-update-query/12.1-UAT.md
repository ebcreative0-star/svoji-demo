---
status: complete
phase: 12.1-ai-update-query
source: 12.1-01-SUMMARY.md, 12.1-02-SUMMARY.md, 12.1-03-SUMMARY.md
started: 2026-03-17T00:00:00Z
updated: 2026-03-17T01:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Checklist Update via Chat
expected: V chatu napiš "přidej datum 28. března k šaty". AI najde existující položku "šaty" a aktualizuje due_date. Žádný duplikát. AI potvrdí update.
result: issue
reported: "napsal jsem: pípu a sudy chci zařídit za dva týdny. Dostal jsem potvrzení že položce je nastaveno datum ale nezměnilo se"
severity: major

### 2. Budget Mark Paid (existující položka)
expected: Přidej rozpočtovou položku. Pak v chatu napiš "zaplatil jsem [název] 12000". Položka se označí jako zaplacená, actual_cost=12000.
result: pass

### 3. Budget Mark Paid (neexistující položka)
expected: V chatu napiš "zaplatil jsem kameramana 15000". AI vytvoří novou položku a rovnou ji označí jako zaplacenou.
result: issue
reported: "odpoved je že bylo zapsáno, ale nic se nestalo"
severity: major

### 4. Disambiguation (více shod)
expected: 2 položky s "fotograf". V chatu "přidej datum k fotografovi". AI nabídne výběr.
result: issue
reported: "AI nepochopila intent, místo disambiguation se ptá na upřesnění data. Používá **bold** a emoji navzdory zákazu."
severity: major

### 5. Tag na nové položce
expected: "přidej fotografa s tagem dodavatel do checklistu". Nová položka se vytvoří s tagem "dodavatel".
result: pass
notes: Tag funguje, ale zobrazuje se i kategorie "Dodavatelé" jako druhý badge vedle tagu.

### 6. Checklist Query
expected: "co mám v checklistu?" -- stručný přehled s reálnými daty, bez bold markupu.
result: issue
reported: "AI vrací jen 8 položek místo reálných 36, stále používá **bold** markup a emoji navzdory zákazu v system promptu"
severity: major

### 7. Budget Query
expected: "kolik mám v rozpočtu?" -- celková částka, zaplaceno, zbývá.
result: pass
notes: Víceméně pass, AI si vymyslela jednu položku. Chat session persistence bug -- poslední session vždy zmizí po přepnutí tabu.

### 8. Guest Query
expected: "kolik hostů potvrdilo?" -- počty na základě reálných dat.
result: issue
reported: "AI tvrdí že nemá data o hostech, odpovídá 'Bohužel nemám žádné informace o tom, kdo potvrdil účast.' Stále bold + emoji."
severity: major

### 9. Status Overview
expected: "jak jsem na tom?" -- cross-domain souhrn s reálnými čísly, bez bold.
result: pass

### 10. Cmd+K Search Modal
expected: Cmd+K otevře search modal, výsledky seskupené podle domény.
result: pass
notes: Kategorie v search výsledcích jsou anglicky (např. "vendor" místo "Dodavatelé").

### 11. Search Tag Matching
expected: Hledání podle tagu najde položky ze všech domén.
result: pass

### 12. Search Result Navigation + Highlight
expected: Klik na výsledek naviguje na view + žlutý flash highlight.
result: pass

### 13. Mobile Search Icon
expected: Na mobilu ikona lupy v headeru, klik otevře search modal.
result: pass
notes: Scroll v chat okně nefunguje myší (cosmetic).

## Summary

total: 13
passed: 8
issues: 5
pending: 0
skipped: 0

## Gaps

- truth: "AI updates existing checklist item due_date when user says 'přidej datum k [položce]'"
  status: failed
  reason: "User reported: AI potvrdí update ale datum se nezměnilo"
  severity: major
  test: 1
  root_cause: "Two issues: (1) parseCzechDate in src/lib/date-utils.ts only handles digit-based relative dates (regex /^za\\s+(\\d+)\\s+t[yý]dn/) -- Czech word numbers like 'dva', 'tři' are not supported, so 'za dva týdny' returns null. (2) updateChecklistItem (action-executor.ts:326-328) falls back to raw string when parseCzechDate returns null: `cleanUpdates.due_date = parsed ?? cleanUpdates.due_date` -- sending 'za dva týdny' as a date value to Supabase, which rejects it silently or errors on the date column. The action returns success:false but the AI may still confirm the update if the error message is ambiguous in the system prompt injection."
  artifacts:
    - src/lib/date-utils.ts (parseCzechDate -- missing Czech word number support: dva=2, tri=3, ctyri=4, etc.)
    - src/lib/ai/action-executor.ts (updateChecklistItem lines 326-328 -- fallback to raw string on parse failure should instead return error)
  missing:
    - parseCzechDate needs CZECH_WORD_NUMBERS map (jedna=1, dva/dve=2, tri=3, ctyri=4, pet=5, sest=6)
    - updateChecklistItem should return error when parseCzechDate returns null for due_date, not silently pass garbage to DB
  debug_session: ""

- truth: "AI creates new budget item and marks as paid when item doesn't exist"
  status: failed
  reason: "User reported: AI odpovídá že bylo zapsáno ale nic se nestalo"
  severity: major
  test: 3
  root_cause: "The markBudgetPaid handler's upsert/create path (action-executor.ts:570-582) is correctly implemented. The most likely cause is classifier misclassification: Haiku may return budget_add instead of budget_mark_paid for some formulations, creating an unpaid item that the user doesn't find when looking for paid items. Secondary possibility: the item IS created but the budget page has no revalidation/real-time subscription -- ChatInterface.tsx doesn't trigger any data refresh after AI actions, and no Supabase realtime subscriptions exist on budget_items. User must manually refresh the budget page to see changes."
  artifacts:
    - src/lib/ai/intent-classifier.ts (classifier prompt has correct examples for budget_mark_paid but Haiku may still misclassify)
    - src/components/dashboard/ChatInterface.tsx (no revalidation/router.refresh after successful action)
    - src/app/api/chat/route.ts (no signal to client that data was mutated)
  missing:
    - API response should include action metadata (intent, success) so client can trigger revalidation
    - ChatInterface should call router.refresh() or emit event when action was performed
    - Server logs needed to confirm which intent classifier actually returned
  debug_session: ""

- truth: "AI disambiguates when multiple items match and offers selection"
  status: failed
  reason: "User reported: AI nepochopila intent, místo disambiguation se ptá na upřesnění data. Používá bold a emoji."
  severity: major
  test: 4
  root_cause: "Classifier misclassification. 'přidej datum k fotografovi' should map to checklist_update per the classifier examples (line 157: exact match). However, Haiku at temperature 0.1 may still return advice_request or small_talk, especially without conversation context. When misclassified, the route.ts pipeline skips action execution entirely, the system prompt gets 'ZADNA AKCE NEBYLA PROVEDENA', and the AI responds conversationally instead of performing the update+disambiguation. The disambiguation logic itself (action-executor.ts:311-317) is correct -- it returns 'Ambiguous' error with item list when multiple matches found. But it never gets called because the intent is wrong."
  artifacts:
    - src/lib/ai/intent-classifier.ts (classifier has correct example at line 157 but Haiku still misclassifies)
    - src/lib/ai/action-executor.ts (updateChecklistItem lines 311-317 -- disambiguation logic is correct but never reached)
  missing:
    - Consider adding a fallback: if main model response suggests an action, re-classify or detect action-like language
    - Lower confidence threshold for checklist_update or add more examples with prepositions (K, k)
    - Server logs needed to confirm actual classifier output for this input
  debug_session: ""

- truth: "AI responds to checklist query with actual data from database (all items, not partial)"
  status: failed
  reason: "User reported: AI vrací jen 8 položek místo 36, stále bold markup a emoji"
  severity: major
  test: 6
  root_cause: "Hard-coded truncation in queryChecklist handler (action-executor.ts:825): `filtered.slice(0, 15)` caps the item list at 15 entries before injecting into system prompt. The AI then further summarizes/truncates to ~8 items in its response. The user has 36 items but the AI only sees max 15, and presents fewer. The query header line (line 835) correctly shows total counts but the item list is truncated. Additionally, the AI uses bold markup and emoji because: (1) system prompt prohibition is weak ('Nepouzivej emoji prehrane' = don't overuse, not don't use), (2) temperature 1.0 in kilo.ts makes the model less likely to follow formatting constraints strictly."
  artifacts:
    - src/lib/ai/action-executor.ts (queryChecklist line 825 -- slice(0, 15) hard truncation)
    - src/lib/kilo.ts (temperature: 1.0 on line 85 -- too high for constrained output)
    - src/app/api/chat/route.ts (buildSystemPrompt line 204 -- weak emoji prohibition 'Nepouzivej emoji prehrane')
  missing:
    - Remove or increase the slice limit -- pass all items (or at least group by category with counts)
    - For large lists, generate a summary format (X items by category) instead of truncated list
    - Strengthen system prompt: 'NIKDY nepouzivej markdown formatovani (bold, italic, headings). NIKDY nepouzivej emoji.'
    - Lower temperature to 0.7 for more consistent instruction following
  debug_session: ""

- truth: "AI responds to guest query with actual RSVP data from database"
  status: failed
  reason: "User reported: AI tvrdí že nemá data, odpovídá 'nemám žádné informace'. Bold + emoji."
  severity: major
  test: 8
  root_cause: "Classifier misclassification. 'kolik hostů potvrdilo?' should map to guest_query (per classifier example line 217). The isQueryIntent function correctly includes guest_query. The queryGuests handler correctly queries the guests table. But if the classifier returns advice_request or small_talk instead of guest_query, the route.ts pipeline never executes the query, no data is injected into the system prompt, and the AI responds from its training data saying it has no information. This is the same class of bug as Test 4 -- Haiku misclassification. Note: Test 9 (status_overview, 'jak jsem na tom?') passes, which uses a different intent that also queries guests -- proving the data exists and the DB query works."
  artifacts:
    - src/lib/ai/intent-classifier.ts (classifier has correct example but Haiku misclassifies)
    - src/app/api/chat/route.ts (pipeline correctly handles guest_query when classified -- the issue is upstream classification)
  missing:
    - Server logs needed to confirm actual classifier output for 'kolik hostů potvrdilo?'
    - Consider client-side intent detection as fallback (regex patterns for common queries)
    - Consider using a stronger model for classification (claude-sonnet instead of haiku) for query intents
  debug_session: ""

## Cross-Cutting Issues

- truth: "AI never uses bold markup (**text**) or emoji in responses"
  status: failed
  reason: "Bold and emoji appear in tests 4, 6, 8 and likely others"
  severity: minor
  root_cause: "Three contributing factors: (1) System prompt in buildSystemPrompt (route.ts:204) says 'Nepouzivej emoji prehrane' which means 'don't overuse emoji' not 'never use emoji'. (2) System prompt line 206 says 'Nepouzivej **tucne** oznaceni textu' which ironically uses bold to say don't use bold. (3) Temperature 1.0 in kilo.ts:85 reduces instruction-following compliance. The chat UI (ChatInterface.tsx:329) renders via whitespace-pre-wrap without markdown parsing, so **bold** shows as literal asterisks."
  artifacts:
    - src/app/api/chat/route.ts (buildSystemPrompt lines 204, 206 -- weak prohibitions)
    - src/lib/kilo.ts (line 85 -- temperature 1.0)
  missing:
    - Replace 'Nepouzivej emoji prehrane' with 'NIKDY nepouzivej emoji.'
    - Replace '**tucne** oznaceni' with 'NIKDY nepouzivej markdown formatovani vcetne **bold**, *italic*, ani headings. Pis pouze cisty text.'
    - Lower temperature from 1.0 to 0.7
    - Optionally add post-processing strip in route.ts to remove ** and emoji before returning
