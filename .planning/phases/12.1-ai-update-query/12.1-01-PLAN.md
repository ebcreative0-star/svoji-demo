---
phase: 12.1-ai-update-query
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/intent-classifier.ts
  - src/lib/ai/action-executor.ts
  - src/app/api/chat/route.ts
autonomous: true
requirements: [INTENT-01, INTENT-02, INTENT-07, INTENT-08, INTENT-09]

must_haves:
  truths:
    - "User says 'pridej datum 15. kvetna k fotografovi' and the existing checklist item is updated (not duplicated)"
    - "User says 'zaplatil jsem fotografa 25000' and budget item is marked paid with actual_cost=25000"
    - "User says 'zaplatil jsem kadeřnici 5000' when item doesn't exist and a new paid item is created"
    - "User says 'pridej catering do checklistu' and the category is set to 'vendors' (not 'other')"
    - "User says 'pridej fotografa s tagem dodavatel' and the checklist item is created with tags=['dodavatel']"
    - "User says 'pridej tag urgent k fotografovi' and the tag is appended to existing tags"
    - "When multiple items match an update query, AI asks user to disambiguate"
  artifacts:
    - path: "src/lib/ai/intent-classifier.ts"
      provides: "checklist_update, budget_mark_paid intents + tag/category examples"
      contains: "checklist_update"
    - path: "src/lib/ai/action-executor.ts"
      provides: "updateChecklistItem, markBudgetPaid handlers"
      contains: "updateChecklistItem"
    - path: "src/app/api/chat/route.ts"
      provides: "Updated isActionIntent check including new intents"
  key_links:
    - from: "src/lib/ai/intent-classifier.ts"
      to: "src/lib/ai/action-executor.ts"
      via: "intent string passed to executeAction switch/case"
      pattern: "case 'checklist_update'"
    - from: "src/lib/ai/action-executor.ts"
      to: "supabase"
      via: "ilike search then .update() or .insert()"
      pattern: "ilike.*title.*%"
---

<objective>
Add update intents (checklist_update, budget_mark_paid) and tag/category awareness to the AI pipeline so the chatbot can modify existing items instead of always creating new ones, and correctly assigns categories and tags when creating items.

Purpose: Users currently get duplicate items when they say "add a date to photographer" because the AI only knows how to create. This plan teaches the AI to update existing items, mark budget items as paid, and use proper categories/tags.

Output: Extended classifier with 2 new action intents + improved examples, 2 new handler functions in action-executor, tags passthrough in existing add handlers.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-ai-update-query/12.1-CONTEXT.md
@.planning/phases/12.1-ai-update-query/12.1-RESEARCH.md

@src/lib/ai/intent-classifier.ts
@src/lib/ai/action-executor.ts
@src/app/api/chat/route.ts
@src/lib/date-utils.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/ai/intent-classifier.ts:
```typescript
export interface IntentResult {
  intent: string;
  confidence: number;
  params: Record<string, any>;
}
export async function classifyIntent(message: string, conversationContext?: string[]): Promise<IntentResult>;
export function isActionIntent(intent: string): boolean;
```

From src/lib/ai/action-executor.ts:
```typescript
export interface ActionResult {
  success: boolean;
  message: string;
  data?: any;
  error?: string;
}
export async function executeAction(supabase: SupabaseClient, coupleId: string, intent: string, params: Record<string, any>): Promise<ActionResult>;
// Internal: normalizeBudgetCategory(category: string | undefined): string
// Internal: CATEGORY_MAP, VALID_BUDGET_CATEGORIES
```

From src/lib/date-utils.ts:
```typescript
export function parseCzechDate(input: string): string | null; // Returns ISO date string or null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update intents and tag/category awareness to classifier</name>
  <files>src/lib/ai/intent-classifier.ts</files>
  <action>
Extend `CLASSIFICATION_SYSTEM_PROMPT` with new intents and examples:

1. Add to "Akce s daty" section:
   - `checklist_update`: Aktualizovat existujici polozku v checklistu (params: { title: string, updates: { due_date?: string, priority?: string, category?: string, description?: string, tags_append?: string[] } })
   - `budget_mark_paid`: Oznacit vydaj jako zaplaceny (params: { name: string, amount: number })

2. Add category guidance to existing `checklist_add` description:
   - "category je JEDNA Z: venue|attire|vendors|guests|decor|admin|ceremony"
   - Add mapping hints: Fotograf/DJ/catering/floristka = vendors, Saty/oblek = attire, Misto/salonek/zamek = venue, Pozvanky/hoste = guests, Dekorace/kytky = decor, Doklady/smlouvy = admin, Obrad/ritualy = ceremony

3. Add tags param to existing `checklist_add` and `budget_add` intent descriptions:
   - checklist_add params: { title: string, category?: string, tags?: string[] }
   - budget_add params: { name: string, amount: number, category?: string, tags?: string[] }

4. Add disambiguation examples to PRIKLADY section:
   - "pridej datum 15. kvetna k fotografovi" -> checklist_update { title: "fotograf", updates: { due_date: "15. kvetna" } }
   - "pridej fotografa do checklistu" -> checklist_add { title: "fotograf", category: "vendors" }
   - "zaplatil jsem fotografa 25000" -> budget_mark_paid { name: "fotograf", amount: 25000 }
   - "pridej fotografa za 25000" -> budget_add { name: "fotograf", amount: 25000, category: "photo" }
   - "nastav prioritu fotografa na vysoka" -> checklist_update { title: "fotograf", updates: { priority: "high" } }
   - "zmen kategorii fotografa na vendors" -> checklist_update { title: "fotograf", updates: { category: "vendors" } }
   - "pridej fotografa s tagem dodavatel" -> checklist_add { title: "fotograf", category: "vendors", tags: ["dodavatel"] }
   - "pridej tag urgent k fotografovi" -> checklist_update { title: "fotograf", updates: { tags_append: ["urgent"] } }
   - "zaplaceno 30000 za kvetiny" -> budget_mark_paid { name: "kvetiny", amount: 30000 }

5. Add PRAVIDLO for update vs create disambiguation:
   - "DULEZITE: Rozlisuj update vs create. 'pridej [neco] K [polozce]' = checklist_update (predlozka K/k). 'pridej [polozku]' = checklist_add. Klicova slova pro update: nastav, uprav, zmen, aktualizuj, pridej datum/tag/prioritu K."
   - "DULEZITE: 'zaplatil jsem [nazev] [castka]' nebo 'zaplaceno [castka] za [nazev]' = budget_mark_paid (ne budget_add)."

6. Update `isActionIntent()` to include 'checklist_update' and 'budget_mark_paid' in the actionIntents array.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Classifier prompt contains checklist_update and budget_mark_paid intents with examples. isActionIntent returns true for both new intents. Category guidance present for checklist_add. Tags param documented for checklist_add and budget_add.</done>
</task>

<task type="auto">
  <name>Task 2: Add update handlers and tag passthrough in action-executor</name>
  <files>src/lib/ai/action-executor.ts, src/app/api/chat/route.ts</files>
  <action>
In action-executor.ts:

1. Add `updateChecklistItem` handler function:
   - Params: { title: string, updates: Record&lt;string, any&gt; }
   - Find item by `ilike('title', '%${title}%')` with `eq('couple_id', coupleId)` (do NOT filter by completed -- user may want to update completed items too)
   - If 0 matches: return { success: false, message: 'Nenasel jsem polozku "${title}" v checklistu' }
   - If >1 matches: return { success: false, message: 'Nasel jsem ${items.length} polozky: ${list}. Kterou myslíš?', error: 'Ambiguous' } -- this lets Sonnet ask the user to disambiguate
   - If 1 match: apply updates with `.update(cleanUpdates).eq('id', item.id)`
   - Handle `due_date` via `parseCzechDate()` -- if parseCzechDate returns null, keep the raw string (Sonnet may have formatted it)
   - Handle `tags_append`: merge with existing `item.tags ?? []`, then set `tags` in the update. Delete `tags_append` key from cleanUpdates before writing.
   - Return success message with item title

2. Add `markBudgetPaid` handler function:
   - Params: { name: string, amount: number }
   - Find item by `ilike('name', '%${name}%')` with `eq('couple_id', coupleId)`
   - If 0 matches: CREATE new item + mark paid (upsert pattern per user decision):
     - Insert with: name, category: normalizeBudgetCategory(name), estimated_cost: amount, actual_cost: amount, paid: true, source: 'ai'
     - Return: 'Vytvoril jsem a oznacil "${name}" jako zaplaceno (${amount} Kc)'
   - If >=1 match: update first match with { paid: true, actual_cost: amount }
     - Preserve existing estimated_cost (do NOT overwrite it)
     - Return: 'Oznacil jsem "${item.name}" jako zaplaceno (${amount} Kc)'
   - Format amounts with .toLocaleString('cs-CZ')

3. Add switch cases in `executeAction`:
   - case 'checklist_update': return await updateChecklistItem(...)
   - case 'budget_mark_paid': return await markBudgetPaid(...)

4. Modify existing `addChecklistItem` to pass through `tags` param:
   - Accept `tags?: string[]` in params
   - Include `tags: params.tags ?? null` in the insert object

5. Modify existing `addBudgetItem` to pass through `tags` param:
   - Accept `tags?: string[]` in params
   - Include `tags: params.tags ?? null` in the insert object

6. Modify existing `addChecklistItems` (multi) to pass through `tags` param similarly.

In route.ts: No changes needed -- the existing pipeline already passes classified params to executeAction, and isActionIntent was updated in Task 1. The Sonnet response will naturally handle disambiguation messages from the action-executor.

Add "Nepouzivej **bold** markup. Pis cisty text s odrazkami a cisly." to the end of the PRAVIDLA section in buildSystemPrompt().
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>updateChecklistItem and markBudgetPaid handlers exist in action-executor. Switch cases added. addChecklistItem and addBudgetItem pass through tags. Budget mark-paid creates new item when not found. Multi-match disambiguation returns descriptive message. Bold markup prohibition in system prompt.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Manual smoke test: Send "pridej datum 15. kvetna k fotografovi" in chat -- should update existing item, not create duplicate
3. Manual smoke test: Send "zaplatil jsem fotografa 25000" -- should mark paid or create+pay
4. Manual smoke test: Send "pridej catering do checklistu" -- category should be "vendors" not "other"
5. Manual smoke test: Send "pridej fotografa s tagem dodavatel" -- item should have tag
</verification>

<success_criteria>
- checklist_update intent correctly classified and executed (item updated, not duplicated)
- budget_mark_paid works for existing items (paid=true, actual_cost set) and non-existing items (created+paid)
- Tag passthrough works on checklist_add and budget_add
- Category hints improve checklist categorization (vendors, venue, etc.)
- Multi-match returns disambiguation message
- No bold markup in AI responses
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ai-update-query/12.1-01-SUMMARY.md`
</output>
