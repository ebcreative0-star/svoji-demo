---
phase: 12-ai-smarts-first-run
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/dashboard/BudgetView.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04

must_haves:
  truths:
    - "User can add a budget item with name, amount, category, and tags from the budget page"
    - "User can click a budget item to expand an inline edit form"
    - "User can edit name, estimated_cost, actual_cost, category, paid status, and tags"
    - "Edits save optimistically and persist to Supabase"
    - "Empty budget shows illustration + CTA + AI chat link"
  artifacts:
    - path: "src/components/dashboard/BudgetView.tsx"
      provides: "Budget view with expand-in-row edit, tags on add form, empty state"
      min_lines: 400
  key_links:
    - from: "src/components/dashboard/BudgetView.tsx"
      to: "src/components/dashboard/TagInput.tsx"
      via: "import TagInput"
      pattern: "TagInput"
    - from: "src/components/dashboard/BudgetView.tsx"
      to: "@supabase/supabase-js"
      via: "supabase.from('budget_items').update"
      pattern: "supabase.*from.*budget_items.*update"
---

<objective>
Add expand-in-row editing, tags support on add form, and empty state to BudgetView.

Purpose: BudgetView already has add + delete. This plan adds the missing edit capability, integrates tags into the add form, and adds empty state.
Output: Updated BudgetView.tsx with edit, tags, empty state
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ai-smarts-first-run/12-CONTEXT.md
@.planning/phases/12-ai-smarts-first-run/12-RESEARCH.md
@.planning/phases/12-ai-smarts-first-run/12-01-SUMMARY.md

@src/components/dashboard/BudgetView.tsx

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/tags.ts:
export function getTagColor(tag: string): { bg: string; text: string };

From src/components/dashboard/TagInput.tsx:
export function TagInput(props: {
  value: string[];
  onChange: (tags: string[]) => void;
  existingTags: string[];
  placeholder?: string;
}): JSX.Element;

<!-- Existing BudgetView types -->
From src/components/dashboard/BudgetView.tsx:
interface BudgetItem {
  id: string;
  category: string;
  name: string;
  estimated_cost: number | null;
  actual_cost: number | null;
  paid: boolean;
  source?: string;
}
// Add to this interface: tags: string[]

interface BudgetViewProps {
  items: BudgetItem[];
  totalBudget: number | null;
  coupleId: string;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tags to BudgetView add form and display tags on items</name>
  <files>src/components/dashboard/BudgetView.tsx</files>
  <action>
    Modify BudgetView.tsx:

    1. **Update BudgetItem interface** -- add `tags: string[]`

    2. **Add tags field to the existing add form**:
       - Add `tags: [] as string[]` to the newItem state
       - Add TagInput component below the existing form fields in the collapsible add form
       - existingTags: derive from `[...new Set(items.flatMap(i => i.tags || []))]`
       - Include tags in the Supabase insert: `tags: newItem.tags`
       - Reset tags to [] after successful add

    3. **Display tags on each budget item row**:
       - After the category badge on each item row, render tag pills
       - Use getTagColor for deterministic colors
       - Style: `inline-flex items-center gap-1 text-xs font-medium rounded-full px-2 py-0.5`
       - Wrap in a flex container so pills don't overflow on mobile

    Import TagInput from './TagInput' and getTagColor from '@/lib/tags'.
  </action>
  <verify>
    <automated>grep -c "TagInput\|getTagColor\|tags:" src/components/dashboard/BudgetView.tsx</automated>
  </verify>
  <done>Add form includes tags field. Existing items display tag pills with colors.</done>
</task>

<task type="auto">
  <name>Task 2: Add expand-in-row edit and empty state to BudgetView</name>
  <files>src/components/dashboard/BudgetView.tsx</files>
  <action>
    Continue modifying BudgetView.tsx:

    1. **Expand-in-row edit**:
       - State: `editingId: string | null`, `editDraft: Partial<BudgetItem>`
       - Add Pencil icon button on each item row (next to existing Trash2)
       - Click Pencil -> `setEditingId(item.id)`, `setEditDraft({...item})`
       - Below item row, AnimatePresence + motion.div (height:auto, overflow-hidden):
         - Name: Input
         - Estimated cost: Input type="number"
         - Actual cost: Input type="number"
         - Category: Select with BUDGET_CATEGORIES
         - Paid: Checkbox or toggle
         - Tags: TagInput
       - Save button ("Ulozit") + Cancel button ("Zrusit")
       - Save handler:
         - Optimistic update items state
         - `supabase.from('budget_items').update({name, estimated_cost, actual_cost, category, paid, tags}).eq('id', id)`
         - Revert on error
       - Cancel: `setEditingId(null)`

    2. **Empty state** (when items.length === 0):
       - PiggyBank icon (large, muted) -- already imported
       - Heading: "Zatim zadny rozpocet"
       - Subtext: "Zacnete pridavat polozky do sveho rozpoctu"
       - Button primary "Pridat prvni polozku" that opens the add form (setShowAddForm(true))
       - Text link: "Nebo to nechte na AI" linking to /chat

    Import Pencil from lucide-react.
    Import AnimatePresence from framer-motion.
  </action>
  <verify>
    <automated>grep -c "editingId\|editDraft\|Ulozit\|Zatim zadny\|AnimatePresence\|Pencil" src/components/dashboard/BudgetView.tsx</automated>
  </verify>
  <done>Budget items have Pencil icon. Clicking expands edit form with all fields. Save persists optimistically. Empty state shows when no items.</done>
</task>

</tasks>

<verification>
- BudgetView add form includes TagInput field
- Each budget item displays tag pills with deterministic colors
- Each item has Pencil and Trash2 icons
- Pencil click expands edit form with AnimatePresence animation
- Edit form has name, estimated_cost, actual_cost, category, paid, tags
- Save updates optimistically + persists to Supabase
- Empty state shows with PiggyBank icon, CTA, and AI link
</verification>

<success_criteria>
- User can add budget items with tags from the add form
- User can click Pencil to edit any budget item inline
- User can modify all fields (name, costs, category, paid, tags) and save
- Empty budget page shows helpful empty state
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-smarts-first-run/12-03-SUMMARY.md`
</output>
