---
phase: 12-ai-smarts-first-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/009_tags_jsonb.sql
  - src/lib/tags.ts
  - src/lib/date-utils.ts
  - src/components/dashboard/TagInput.tsx
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "Tags column exists on all three item tables (text[] with empty default)"
    - "Tag color is deterministic from tag string (same tag always gets same color)"
    - "TagInput supports adding/removing tags with autocomplete from existing tags"
    - "Czech date strings like '31.1.2026', 'za 2 tydny', 'konec ledna' parse to valid ISO dates"
  artifacts:
    - path: "supabase/migrations/009_tags_jsonb.sql"
      provides: "tags text[] column on checklist_items, budget_items, guests"
      contains: "ALTER TABLE"
    - path: "src/lib/tags.ts"
      provides: "getTagColor() deterministic hash function + TAG_COLORS palette"
      exports: ["getTagColor", "TAG_COLORS"]
    - path: "src/lib/date-utils.ts"
      provides: "parseCzechDate() function for AI chat date normalization"
      exports: ["parseCzechDate"]
    - path: "src/components/dashboard/TagInput.tsx"
      provides: "Reusable tag input with autocomplete, add/remove, colored pills"
      exports: ["TagInput"]
  key_links:
    - from: "src/components/dashboard/TagInput.tsx"
      to: "src/lib/tags.ts"
      via: "import getTagColor"
      pattern: "getTagColor"
    - from: "src/lib/date-utils.ts"
      to: "date-fns"
      via: "import addDays, addWeeks, endOfMonth, parse"
      pattern: "from 'date-fns'"
---

<objective>
Create the foundation layer for Phase 12: DB migration for tags, tag utility functions, TagInput component, and Czech date parser.

Purpose: All three view plans (02, 03, 04) depend on the tags migration and TagInput component. The Czech date parser (FIX-01) is a standalone utility that fits naturally here.
Output: Migration SQL, tags.ts, date-utils.ts, TagInput.tsx
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ai-smarts-first-run/12-CONTEXT.md
@.planning/phases/12-ai-smarts-first-run/12-RESEARCH.md

<interfaces>
<!-- Existing UI components used by TagInput -->

From src/components/ui/Badge.tsx:
Uses cva with intents: success, warning, danger, info, neutral
Sizes: sm, md

From src/components/ui/Input.tsx:
Props: label, error, helperText, all standard HTML input props

From src/lib/checklist-generator.ts:
export type TaskCategory = 'venue' | 'attire' | 'vendors' | 'guests' | 'decor' | 'admin' | 'ceremony';
export type TaskPriority = 'high' | 'medium' | 'low';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tag utilities and Czech date parser</name>
  <files>src/lib/tags.ts, src/lib/date-utils.ts</files>
  <behavior>
    - getTagColor('venue') returns consistent {bg, text} Tailwind classes
    - getTagColor('venue') called twice returns identical result
    - Two different tags may get different colors from the palette
    - parseCzechDate('31.1.2026') returns '2026-01-31'
    - parseCzechDate('2026-01-31') returns '2026-01-31' (passthrough)
    - parseCzechDate('za 2 tydny') returns date 14 days from today
    - parseCzechDate('za 3 dny') returns date 3 days from today
    - parseCzechDate('konec ledna') returns end of January
    - parseCzechDate(undefined) returns null
    - parseCzechDate('random nonsense') returns null
  </behavior>
  <action>
    Create `src/lib/tags.ts`:
    - TAG_COLORS array with 8 color pairs: rose, violet, sky, emerald, amber, pink, indigo, teal (each has bg + text Tailwind classes like 'bg-rose-100' / 'text-rose-700')
    - `getTagColor(tag: string)` -- deterministic hash from char codes, mod TAG_COLORS.length
    - Export both TAG_COLORS and getTagColor

    Create `src/lib/date-utils.ts`:
    - `parseCzechDate(input: string | undefined): string | null`
    - Handle: ISO passthrough, "za N tydnu/dni", "konec [mesice]", "D.M.YYYY", "D.M." short format
    - Use date-fns: parse, addDays, addWeeks, endOfMonth (already installed)
    - Import cs locale from date-fns/locale
    - Return ISO date string (YYYY-MM-DD) or null

    Note: No test framework exists in project. Verify via Node.js inline eval (see verify).
  </action>
  <verify>
    <automated>node -e "const { getTagColor } = require('./src/lib/tags.ts'); console.log(getTagColor('venue'));" 2>/dev/null || echo "Check: files exist and export correctly"; ls -la src/lib/tags.ts src/lib/date-utils.ts</automated>
  </verify>
  <done>Both files exist with correct exports. getTagColor returns deterministic colors. parseCzechDate handles Czech date formats and edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Create TagInput component and DB migration</name>
  <files>src/components/dashboard/TagInput.tsx, supabase/migrations/009_tags_jsonb.sql</files>
  <action>
    Create `supabase/migrations/009_tags_jsonb.sql`:
    ```sql
    ALTER TABLE checklist_items ADD COLUMN IF NOT EXISTS tags text[] DEFAULT '{}';
    ALTER TABLE budget_items ADD COLUMN IF NOT EXISTS tags text[] DEFAULT '{}';
    ALTER TABLE guests ADD COLUMN IF NOT EXISTS tags text[] DEFAULT '{}';
    ```
    Note: Apply via Supabase Dashboard SQL Editor (same as migration 008).

    Create `src/components/dashboard/TagInput.tsx`:
    - Props: `{ value: string[], onChange: (tags: string[]) => void, existingTags: string[], placeholder?: string }`
    - Render existing tags as colored pills (using getTagColor from src/lib/tags.ts) with X button to remove
    - Text input field after pills
    - On typing: show filtered suggestions dropdown (existingTags that match prefix and aren't already selected)
    - Enter or comma key: add current input as new tag (trimmed, lowercased)
    - Backspace on empty input: remove last tag
    - Escape: dismiss suggestions
    - Click suggestion: add that tag
    - Style: flex-wrap container, pills with `inline-flex items-center gap-1 text-xs font-medium rounded-full px-2 py-0.5` + color from getTagColor
    - Suggestion dropdown: absolute positioned, white bg, shadow, rounded, max-h-32 overflow-y-auto
    - Prevent form submit on Enter (e.preventDefault when adding tag)
    - Use 'use client' directive
  </action>
  <verify>
    <automated>ls -la src/components/dashboard/TagInput.tsx supabase/migrations/009_tags_jsonb.sql && grep -c "export" src/components/dashboard/TagInput.tsx</automated>
  </verify>
  <done>TagInput component renders tag pills with colors, supports add/remove/autocomplete. Migration file ready for manual application in Supabase Dashboard.</done>
</task>

</tasks>

<verification>
- src/lib/tags.ts exports getTagColor and TAG_COLORS
- src/lib/date-utils.ts exports parseCzechDate
- src/components/dashboard/TagInput.tsx exports TagInput component
- supabase/migrations/009_tags_jsonb.sql contains ALTER TABLE for all 3 tables
- No new npm dependencies added (all using existing date-fns, lucide-react)
</verification>

<success_criteria>
- Foundation files exist and are importable by downstream plans (02, 03, 04)
- Czech date parser handles "31.1.2026" -> "2026-01-31", relative dates, and edge cases
- TagInput is a self-contained component ready to drop into edit forms
- Migration SQL is ready for manual application
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-smarts-first-run/12-01-SUMMARY.md`
</output>
