---
phase: 12-ai-smarts-first-run
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/dashboard/GuestsView.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06

must_haves:
  truths:
    - "User can add a guest with tags from the add form"
    - "User can click a guest row to expand an inline edit form"
    - "User can edit name, group, RSVP status, dietary requirements, plus_one, and tags"
    - "Edits save optimistically and persist to Supabase"
    - "Empty guests list shows illustration + CTA + AI chat link"
    - "Guest list works as div-based layout (not HTML table) for reliable expand animation"
  artifacts:
    - path: "src/components/dashboard/GuestsView.tsx"
      provides: "Guests view with div layout, expand-in-row edit, tags, empty state"
      min_lines: 450
  key_links:
    - from: "src/components/dashboard/GuestsView.tsx"
      to: "src/components/dashboard/TagInput.tsx"
      via: "import TagInput"
      pattern: "TagInput"
    - from: "src/components/dashboard/GuestsView.tsx"
      to: "@supabase/supabase-js"
      via: "supabase.from('guests').update"
      pattern: "supabase.*from.*guests.*update"
---

<objective>
Refactor GuestsView from table to div layout, add expand-in-row editing, tags on add form, and empty state.

Purpose: GuestsView uses an HTML table which breaks Framer Motion height animation. Convert to div layout first, then add edit and tags. Already has add + delete.
Output: Updated GuestsView.tsx with div layout, edit, tags, empty state
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ai-smarts-first-run/12-CONTEXT.md
@.planning/phases/12-ai-smarts-first-run/12-RESEARCH.md
@.planning/phases/12-ai-smarts-first-run/12-01-SUMMARY.md

@src/components/dashboard/GuestsView.tsx

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/tags.ts:
export function getTagColor(tag: string): { bg: string; text: string };

From src/components/dashboard/TagInput.tsx:
export function TagInput(props: {
  value: string[];
  onChange: (tags: string[]) => void;
  existingTags: string[];
  placeholder?: string;
}): JSX.Element;

<!-- Existing GuestsView types -->
From src/components/dashboard/GuestsView.tsx:
interface Guest {
  id: string;
  name: string;
  email: string | null;
  phone: string | null;
  group_name: string | null;
  plus_one: boolean;
  dietary_requirements: string | null;
  rsvp_status: 'pending' | 'confirmed' | 'declined';
  notes: string | null;
}
// Add to this interface: tags: string[]

interface GuestsViewProps {
  guests: Guest[];
  coupleId: string;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor GuestsView from table to div layout + add tags</name>
  <files>src/components/dashboard/GuestsView.tsx</files>
  <action>
    Modify GuestsView.tsx:

    1. **Update Guest interface** -- add `tags: string[]`

    2. **Convert table to div-based layout**:
       - Replace `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` with div equivalents
       - Header row: `<div className="hidden md:grid md:grid-cols-[1fr_120px_100px_100px_80px] gap-4 px-4 py-2 text-sm font-medium text-[var(--color-text-light)] border-b">` with column headers: Jmeno, Skupina, RSVP, Diety, Akce
       - Each guest row: `<div className="grid grid-cols-1 md:grid-cols-[1fr_120px_100px_100px_80px] gap-2 md:gap-4 items-center px-4 py-3 border-b hover:bg-[var(--color-bg-warm)]">`
       - Mobile: stack vertically (grid-cols-1), show name + RSVP badge + action icons
       - Desktop: horizontal grid matching header columns
       - Keep existing RSVP status buttons (confirmed/pending/declined) -- same functionality
       - Keep existing delete (Trash2) icon
       - Keep existing search + filter above the list
       - Keep CSV export button

    3. **Add tags to existing add form**:
       - Add `tags: [] as string[]` to newGuest state
       - Add TagInput below existing form fields in the collapsible add section
       - existingTags: `[...new Set(guests.flatMap(g => g.tags || []))]`
       - Include tags in Supabase insert
       - Reset tags after successful add

    4. **Display tags on each guest row**:
       - Below the guest name (or after it on desktop), render tag pills using getTagColor
       - Keep compact on mobile

    Import TagInput from './TagInput' and getTagColor from '@/lib/tags'.

    IMPORTANT: The table-to-div refactor must preserve all existing functionality: search, filter, RSVP toggle, delete, add form, CSV export, stats cards. Only the rendering structure changes from table elements to div grid.
  </action>
  <verify>
    <automated>grep -c "table\|thead\|tbody" src/components/dashboard/GuestsView.tsx | xargs -I{} test {} -eq 0 && echo "No table elements" || echo "Still has table elements"; grep -c "TagInput\|getTagColor" src/components/dashboard/GuestsView.tsx</automated>
  </verify>
  <done>GuestsView uses div grid layout (no table elements). Tags on add form and displayed on rows. All existing features preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add expand-in-row edit and empty state to GuestsView</name>
  <files>src/components/dashboard/GuestsView.tsx</files>
  <action>
    Continue modifying GuestsView.tsx:

    1. **Expand-in-row edit**:
       - State: `editingId: string | null`, `editDraft: Partial<Guest>`
       - Add Pencil icon button in each guest row's action column (next to Trash2)
       - Click Pencil -> `setEditingId(guest.id)`, `setEditDraft({...guest})`
       - Below the guest row div, AnimatePresence + motion.div (height:auto, overflow-hidden):
         - Name: Input
         - Email: Input type="email"
         - Phone: Input type="tel"
         - Group: Input (or Select with common group names)
         - RSVP status: Select with pending/confirmed/declined
         - Dietary requirements: Input
         - Plus one: Checkbox
         - Notes: Textarea (small)
         - Tags: TagInput
       - Save button ("Ulozit") + Cancel button ("Zrusit")
       - Save handler:
         - Optimistic update guests state
         - `supabase.from('guests').update({name, email, phone, group_name, rsvp_status, dietary_requirements, plus_one, notes, tags}).eq('id', guest.id)`
         - Revert on error, close edit form on success
       - Cancel: `setEditingId(null)`

    2. **Empty state** (when guests.length === 0):
       - Users icon (large, muted) from lucide-react -- already imported
       - Heading: "Zatim zadni hoste"
       - Subtext: "Zacnete pridavat hosty na svatbu"
       - Button primary "Pridat prvniho hosta" that opens add form (setShowAddForm(true))
       - Text link: "Nebo to nechte na AI" linking to /chat

    Import Pencil from lucide-react.
    Import AnimatePresence from framer-motion.
  </action>
  <verify>
    <automated>grep -c "editingId\|editDraft\|Ulozit\|Zatim zadni\|AnimatePresence\|Pencil" src/components/dashboard/GuestsView.tsx</automated>
  </verify>
  <done>Guest rows have Pencil icon. Clicking expands edit form with all guest fields + tags. Save persists optimistically. Empty state shows when no guests.</done>
</task>

</tasks>

<verification>
- GuestsView uses div-based grid layout (no table/thead/tbody/tr/td elements)
- Guest add form includes TagInput field
- Each guest displays tag pills
- Each guest row has Pencil and Trash2 icons
- Pencil click expands edit form with AnimatePresence
- Edit form has all guest fields: name, email, phone, group, RSVP, dietary, plus_one, notes, tags
- Save updates optimistically + persists
- Empty state shows with Users icon, CTA, and AI link
- Existing features preserved: search, filter, RSVP toggle, delete, CSV export
</verification>

<success_criteria>
- GuestsView renders as div grid (table elements removed)
- User can add guests with tags
- User can edit any guest inline with expand-in-row pattern
- All guest fields editable: name, email, phone, group, RSVP, dietary, plus_one, notes, tags
- Empty guest list shows helpful empty state
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-smarts-first-run/12-04-SUMMARY.md`
</output>
