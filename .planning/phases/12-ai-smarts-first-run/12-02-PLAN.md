---
phase: 12-ai-smarts-first-run
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/dashboard/ChecklistView.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "User can type a task title and press Enter to add a checklist item"
    - "User can click a checklist item to expand an inline edit form with title, due_date, priority, category, and tags"
    - "User can save edits and see updated values immediately (optimistic)"
    - "User can delete a checklist item via trash icon"
    - "Empty checklist shows illustration + CTA + AI chat link"
  artifacts:
    - path: "src/components/dashboard/ChecklistView.tsx"
      provides: "Full CRUD checklist view with quick-add, expand-in-row edit, delete, empty state"
      min_lines: 500
  key_links:
    - from: "src/components/dashboard/ChecklistView.tsx"
      to: "src/components/dashboard/TagInput.tsx"
      via: "import TagInput"
      pattern: "TagInput"
    - from: "src/components/dashboard/ChecklistView.tsx"
      to: "@supabase/supabase-js"
      via: "supabase.from('checklist_items').insert/update/delete"
      pattern: "supabase.*from.*checklist_items"
---

<objective>
Add full CRUD capabilities to ChecklistView: quick-add field, expand-in-row editing, delete, tags display, and empty state.

Purpose: ChecklistView is currently read-only (only toggle completion). This is the biggest CRUD gap in the dashboard.
Output: Updated ChecklistView.tsx with add, edit, delete, tags, empty state
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ai-smarts-first-run/12-CONTEXT.md
@.planning/phases/12-ai-smarts-first-run/12-RESEARCH.md
@.planning/phases/12-ai-smarts-first-run/12-01-SUMMARY.md

@src/components/dashboard/ChecklistView.tsx

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/tags.ts:
export function getTagColor(tag: string): { bg: string; text: string };

From src/components/dashboard/TagInput.tsx:
export function TagInput(props: {
  value: string[];
  onChange: (tags: string[]) => void;
  existingTags: string[];
  placeholder?: string;
}): JSX.Element;

<!-- Existing ChecklistView types -->
From src/components/dashboard/ChecklistView.tsx:
interface ChecklistItem {
  id: string;
  title: string;
  description: string | null;
  category: string;
  due_date: string;
  priority: string;
  completed: boolean;
  completed_at: string | null;
}
// Add to this interface: tags: string[]

interface ChecklistViewProps {
  items: ChecklistItem[];
  weddingDate: string;
}
// Add to this interface: coupleId: string (needed for insert)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add quick-add and delete to ChecklistView</name>
  <files>src/components/dashboard/ChecklistView.tsx</files>
  <action>
    Modify ChecklistView.tsx to add:

    1. **Update ChecklistItem interface** -- add `tags: string[]` field (optional with `|| []` fallback in renders)

    2. **Update ChecklistViewProps** -- add `coupleId: string` prop (needed for Supabase insert)

    3. **Quick-add field at top of view** (above the stats cards):
       - State: `quickTitle: string`, `showDetails: boolean`
       - Single Input with placeholder "Pridat ukol..." and onKeyDown Enter handler
       - Below input: clickable text "+ Detaily (datum, priorita, stitky)" that toggles expandable section
       - Expandable section (AnimatePresence + motion.div with height:auto, overflow-hidden):
         - Date field: `<input type="date">` for due_date
         - Priority: Select with high/medium/low options
         - Category: Select with existing CATEGORY_LABELS
         - Tags: TagInput component with existingTags derived from all items
         - Add button (Button primary) to submit with all details
       - Quick Enter submit: inserts with defaults (priority: 'medium', category: 'admin', due_date: null, tags: [])
       - Supabase insert: `supabase.from('checklist_items').insert({couple_id: coupleId, title, due_date, priority, category, tags}).select().single()`
       - Optimistic update: add to items state immediately, revert on error

    4. **Delete button on each item row**:
       - Add Trash2 icon button (ghost/danger variant) at the right of each item row
       - On click: optimistic remove from items state + `supabase.from('checklist_items').delete().eq('id', id)`
       - Revert on error

    5. **Null guard for due_date in groupItems**:
       - In the groupBy === 'date' branch, add at top: `if (!item.due_date) { return { groupId: 'noDueDate', label: 'Bez terminu' }; }`
       - This prevents Invalid Date when items have null due_date

    6. **Display tags on each item row**:
       - After the category Badge, render tag pills: `(item.tags || []).map(tag => <span> with getTagColor classes)`

    Import TagInput from './TagInput' and getTagColor from '@/lib/tags'.
    Import Trash2, Pencil from lucide-react.
  </action>
  <verify>
    <automated>grep -c "Pridat ukol\|quickTitle\|Trash2\|handleDelete\|noDueDate" src/components/dashboard/ChecklistView.tsx</automated>
  </verify>
  <done>Quick-add field works with Enter key. Delete trash icon on each row. Tags display as colored pills. Null due_date handled in grouping.</done>
</task>

<task type="auto">
  <name>Task 2: Add expand-in-row edit and empty state to ChecklistView</name>
  <files>src/components/dashboard/ChecklistView.tsx</files>
  <action>
    Continue modifying ChecklistView.tsx:

    1. **Expand-in-row edit pattern**:
       - State: `editingId: string | null`, `editDraft: Partial<ChecklistItem>`
       - Click Pencil icon on item row -> `setEditingId(item.id)` + `setEditDraft({...item})`
       - Below the item row (inside same list item container), use AnimatePresence:
         ```
         {editingId === item.id && (
           <motion.div initial={{height:0, opacity:0}} animate={{height:'auto', opacity:1}} exit={{height:0, opacity:0}} transition={{duration:0.2}} className="overflow-hidden">
             // Edit form
           </motion.div>
         )}
         ```
       - Edit form fields:
         - Title: Input with value={editDraft.title}
         - Due date: `<input type="date" value={editDraft.due_date || ''} />`
         - Priority: Select (high/medium/low)
         - Category: Select (CATEGORY_LABELS)
         - Tags: TagInput with value={editDraft.tags || []}
       - Save button (Button primary "Ulozit") + Cancel button (Button ghost "Zrusit")
       - Save: optimistic update items state + `supabase.from('checklist_items').update({title, due_date, priority, category, tags}).eq('id', id)`, revert on error
       - Cancel: `setEditingId(null)`

    2. **Empty state** (when items.length === 0):
       - Show instead of the item list
       - Center-aligned container with:
         - ClipboardList icon (large, muted color) from lucide-react
         - Heading: "Zatim zadne ukoly"
         - Subtext: "Zacnete pridavat ukoly do sveho checklistu"
         - Button primary "Pridat prvni ukol" that focuses the quick-add input
         - Text link: "Nebo to nechte na AI" linking to /chat

    3. **Pencil icon** on each item row (next to Trash2):
       - Small ghost button with Pencil icon
       - onClick: startEdit(item)

    Ensure AnimatePresence is imported from framer-motion.
    Ensure only one item can be edited at a time (setting editingId replaces previous).
  </action>
  <verify>
    <automated>grep -c "editingId\|editDraft\|Ulozit\|Zrusit\|Zatim zadne\|AnimatePresence" src/components/dashboard/ChecklistView.tsx</automated>
  </verify>
  <done>Clicking Pencil expands edit form in-row with smooth animation. Save persists to Supabase with optimistic update. Empty state shows illustration + CTA + AI link.</done>
</task>

</tasks>

<verification>
- ChecklistView has quick-add input at top with Enter-to-submit
- ChecklistView has expandable details section below quick-add
- Each checklist item has Pencil (edit) and Trash2 (delete) icons
- Clicking Pencil expands edit form with AnimatePresence animation
- Edit form has title, due_date, priority, category, tags fields
- Save updates optimistically + persists to Supabase
- Delete removes optimistically + persists to Supabase
- Empty state shows when no items exist
- Tags display as colored pills on each item
- Null due_date items grouped under "Bez terminu"
</verification>

<success_criteria>
- User can add a checklist item by typing title + Enter (quick-add)
- User can add with full details (date, priority, category, tags) via expanded form
- User can click Pencil to expand inline edit with all fields
- User can save or cancel edits
- User can delete items via trash icon
- Empty checklist shows helpful empty state with CTA
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-smarts-first-run/12-02-SUMMARY.md`
</output>
