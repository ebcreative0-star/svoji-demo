---
phase: 08-ai-pipeline
plan: 06
type: execute
wave: 2
depends_on: [08-05]
files_modified: []
autonomous: false
gap_closure: true
requirements: [AI-03]

must_haves:
  truths:
    - "Rate limiting enforces 50 messages/day with warning at 45"
    - "increment_chat_count function exists in Supabase DB"
    - "demand_signals table exists in Supabase DB"
  artifacts:
    - path: "supabase/migrations/005_demand_signals.sql"
      provides: "Demand signals table DDL"
      contains: "create table"
    - path: "supabase/migrations/006_rate_limits.sql"
      provides: "Rate limit table and atomic increment function"
      contains: "increment_chat_count"
  key_links:
    - from: "src/lib/rate-limit.ts"
      to: "supabase increment_chat_count function"
      via: "supabase.rpc('increment_chat_count')"
      pattern: "increment_chat_count"
---

<objective>
Apply the two missing Supabase migrations (demand_signals + rate_limits) and verify the full AI pipeline works end-to-end.

Purpose: Rate limiting silently fails because the DB function does not exist yet. Demand signals table is also missing.

Output: Migrations applied, rate limiting functional, AI chat actions working correctly.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-pipeline/08-UAT.md
@supabase/migrations/005_demand_signals.sql
@supabase/migrations/006_rate_limits.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Supabase migrations via CLI</name>
  <files></files>
  <action>
Apply the two pending migrations to the Supabase instance. Try using the Supabase CLI first:

```bash
npx supabase db push
```

If the CLI is not configured or fails, try direct SQL execution via the Supabase management API. The migrations are:

1. `supabase/migrations/005_demand_signals.sql` - Creates demand_signals table with RLS
2. `supabase/migrations/006_rate_limits.sql` - Creates chat_rate_limits table and increment_chat_count function

If CLI approaches fail, create a checkpoint for the user to apply via Supabase Dashboard SQL Editor. Print the SQL contents so the user can copy-paste.

After applying, verify the function exists:
```bash
npx supabase db inspect --function increment_chat_count 2>/dev/null || echo "Verify manually"
```
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && npx supabase db push --dry-run 2>&1 | head -20 || echo "Check Supabase dashboard manually"</automated>
  </verify>
  <done>Both migrations applied. increment_chat_count function exists in DB. demand_signals table exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full AI pipeline end-to-end</name>
  <files></files>
  <action>
Present the UAT re-test checklist to the user. All Phase 8 gap closures have been applied:
1. Intent classification improved with few-shot examples (Plan 05)
2. AI no longer fabricates action confirmations (Plan 05)
3. DB migrations applied for rate limiting and demand signals (Task 1)
4. Earlier hotfixes committed: model ID fix, JSON code fence stripping, RLS auth check
  </action>
  <verify>User confirms all tests pass at checkpoint</verify>
  <done>User approves that all 8 UAT tests pass</done>
</task>

</tasks>

<verification>
1. All 8 UAT tests from 08-UAT.md now pass
2. Intent classification correctly handles Czech action phrases
3. AI only confirms actions when actionResult is present
4. Rate limiting DB function exists and works
</verification>

<success_criteria>
- UAT tests 1-8 all pass
- No AI fabrication of action confirmations
- Rate limiting functional (warning at 45, block at 50)
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-pipeline/08-06-SUMMARY.md`
</output>
