# Plan 08-01: Kilo Gateway Integration

## Goal
Replace direct Anthropic SDK calls with Kilo Gateway API for all AI chat requests.

## Context
- Current: `src/app/api/chat/route.ts` uses `@anthropic-ai/sdk` directly
- Target: Use Kilo Gateway (`https://api.kilo.ai/api/gateway/chat/completions`)
- Kilo API key already in `.env.local` as `KILO_API_KEY`

## Tasks

### 1. Create Kilo client utility
**File:** `src/lib/kilo.ts`
```typescript
// OpenAI-compatible client for Kilo Gateway
// - Base URL: https://api.kilo.ai/api/gateway
// - Model: anthropic/claude-sonnet-4.5
// - Auth: Bearer token from KILO_API_KEY
// - Streaming support via SSE
```

### 2. Refactor chat route
**File:** `src/app/api/chat/route.ts`
- Remove `@anthropic-ai/sdk` import
- Import Kilo client
- Convert message format (Anthropic -> OpenAI style)
- Handle response format (OpenAI -> current format)
- Keep all existing logic (auth, history, DB save)

### 3. Update environment config
**File:** `.env.example`
- Add `KILO_API_KEY` placeholder
- Remove/deprecate `ANTHROPIC_API_KEY` reference

### 4. Remove Anthropic SDK dependency
```bash
npm uninstall @anthropic-ai/sdk
```

## Verification
- [ ] Chat works end-to-end with Kilo Gateway
- [ ] Response quality unchanged
- [ ] Error handling works (API down, rate limit, etc.)
- [ ] No `@anthropic-ai/sdk` imports remain in codebase

## Dependencies
- Kilo API key configured (done)
- No other phase dependencies

## Estimated Effort
2-3 hours
