---
phase: 12.1.1-nlp-js-intent-classifier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/ai/training-data.json
  - src/lib/ai/nlp-classifier.ts
  - src/lib/ai/__tests__/nlp-classifier.test.ts
autonomous: true
requirements:
  - INTENT-01
  - INTENT-02
  - INTENT-03
  - INTENT-04
  - INTENT-06

must_haves:
  truths:
    - "NLP.js classifies 'pridej datum k fotografovi' as checklist_update with confidence >= 0.5"
    - "NLP.js classifies 'zaplatil jsem kameramana 15000' as budget_mark_paid with confidence >= 0.5"
    - "NLP.js classifies 'co mam v checklistu?' as checklist_query with confidence >= 0.5"
    - "NLP.js classifies 'kolik hostu potvrdilo?' as guest_query with confidence >= 0.5"
    - "NLP.js classification completes in under 50ms after warm start"
  artifacts:
    - path: "src/lib/ai/nlp-classifier.ts"
      provides: "NLP.js singleton with train/classify API"
      exports: ["getNlpClassifier", "NlpClassifier"]
    - path: "src/lib/ai/training-data.json"
      provides: "NLP.js corpus with ~150-200 Czech utterances"
      contains: "checklist_update"
    - path: "src/lib/ai/__tests__/nlp-classifier.test.ts"
      provides: "Test scaffold covering INTENT-01 through INTENT-06"
  key_links:
    - from: "src/lib/ai/nlp-classifier.ts"
      to: "src/lib/ai/training-data.json"
      via: "JSON import for corpus"
      pattern: "import.*training-data"
---

<objective>
Build the NLP.js classifier module, training corpus, and test scaffold. This plan creates the standalone NLP.js infrastructure that Plan 02 will integrate into the existing intent-classifier.ts.

Purpose: Establish a working, tested NLP.js classifier with Czech language support before wiring it into the production pipeline.
Output: nlp-classifier.ts singleton, training-data.json corpus (~150-200 utterances), passing test file.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1.1-nlp-js-intent-classifier/12.1.1-CONTEXT.md
@.planning/phases/12.1.1-nlp-js-intent-classifier/12.1.1-RESEARCH.md
@.planning/phases/12.1.1-nlp-js-intent-classifier/12.1.1-VALIDATION.md

@src/lib/ai/intent-classifier.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/ai/intent-classifier.ts:
```typescript
export interface IntentResult {
  intent: string;
  confidence: number;
  params: Record<string, any>;
}
```

Intent names (must match exactly in training-data.json):
- Action intents: checklist_add, checklist_add_multi, checklist_complete, checklist_remove, checklist_update, budget_add, budget_add_multi, budget_update, budget_remove, budget_mark_paid, guest_add, guest_add_multi, guest_update, guest_remove
- Query intents: checklist_query, budget_query, guest_query, status_overview
- Info intents: vendor_search, advice_request, small_talk
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install NLP.js packages and create test scaffold</name>
  <files>package.json, src/lib/ai/__tests__/nlp-classifier.test.ts</files>
  <behavior>
    - Test: "pridej datum k fotografovi" classifies as checklist_update (>= 0.5 confidence)
    - Test: "zaplatil jsem kameramana 15000" classifies as budget_mark_paid (>= 0.5 confidence)
    - Test: "co mam v checklistu?" classifies as checklist_query (>= 0.5 confidence)
    - Test: "kolik hostu potvrdilo?" classifies as guest_query (>= 0.5 confidence)
    - Test: Classification of any warm-start message takes < 50ms
    - Test: All 20 intent names are present in training-data.json
  </behavior>
  <action>
    1. Install packages: `npm install @nlpjs/basic@4.27.0 @nlpjs/lang-cs@4.26.1`
    2. Create `src/lib/ai/__tests__/nlp-classifier.test.ts` using bare `assert` + `process.exit(1)` pattern (no test framework). Tests import from `../nlp-classifier` and `../training-data.json`. Each test:
       - Calls `getNlpClassifier()`, then `classify(text)`
       - Asserts intent name matches expected
       - Asserts confidence >= 0.5
       - For latency test: measure Date.now() around classify call, assert < 50ms (run after init to test warm path)
       - For coverage test: load training-data.json, check all 20 intent names are present in data[].intent
    3. Tests should FAIL initially (RED) since nlp-classifier.ts and training-data.json don't exist yet.
  </action>
  <verify>
    <automated>npx tsx src/lib/ai/__tests__/nlp-classifier.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>@nlpjs/basic and @nlpjs/lang-cs in package.json dependencies. Test file exists and will pass once Task 2 creates the classifier + corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Create training corpus and NLP.js classifier module</name>
  <files>src/lib/ai/training-data.json, src/lib/ai/nlp-classifier.ts</files>
  <action>
    1. Create `src/lib/ai/training-data.json` in NLP.js corpus format:
       ```json
       {
         "name": "Svoji Intent Corpus",
         "locale": "cs",
         "data": [
           { "intent": "checklist_update", "utterances": [...] },
           ...
         ]
       }
       ```
       - Convert ALL examples from CLASSIFICATION_SYSTEM_PROMPT (the "PRIKLADY:" section in intent-classifier.ts) into training utterances. Each example's user message becomes an utterance for its intent.
       - Convert ALL keyword fallback regex patterns into explicit utterances (e.g., "kolik hostu potvrdilo", "kdo potvrdil", "co mam v checklistu").
       - Add 3-5 variants per intent covering: (a) with diacritics, (b) without diacritics, (c) colloquial/short form, (d) different word order.
       - Target: ~150-200 total utterances across all 20 intents.
       - Include advice_request and small_talk intents with enough utterances (5-8 each) so short ambiguous messages ("OK", "diky", "super") score below 0.5 on action intents.
       - Intent names MUST match exactly: checklist_add, checklist_add_multi, checklist_complete, checklist_remove, checklist_update, budget_add, budget_add_multi, budget_update, budget_remove, budget_mark_paid, guest_add, guest_add_multi, guest_update, guest_remove, checklist_query, budget_query, guest_query, status_overview, vendor_search, advice_request, small_talk.

    2. Create `src/lib/ai/nlp-classifier.ts`:
       - Add `import 'server-only'` at top (prevents client/edge bundling).
       - Use singleton pattern from RESEARCH.md Pattern 1: module-level + `global._nlpClassifier` for dev hot reload.
       - `dockStart({ use: ['Basic'] })`, register `LangCs`, `addLanguage('cs')`, load corpus from training-data.json, `train()`.
       - `classify(text: string)` method: calls `nlp.process('cs', text)`, returns `{ intent: result.intent, confidence: result.score, entities: result.entities ?? [] }`. NOTE: use `result.score` NOT `result.confidence` (Pitfall 1 from research).
       - Lazy init: first call to `classify()` triggers `init()` if not ready.
       - Export `getNlpClassifier()` function and the class type.

    3. Run tests to verify all pass (GREEN).
  </action>
  <verify>
    <automated>npx tsx src/lib/ai/__tests__/nlp-classifier.test.ts</automated>
  </verify>
  <done>All tests pass: 4 intent classification tests correct, latency < 50ms on warm path, all 20 intents present in corpus. NLP.js classifier module exports getNlpClassifier().</done>
</task>

</tasks>

<verification>
- `npx tsx src/lib/ai/__tests__/nlp-classifier.test.ts` exits 0 with all assertions passing
- training-data.json has >= 150 utterances across all 20 intent types
- nlp-classifier.ts has `import 'server-only'` guard
- No changes to intent-classifier.ts yet (that's Plan 02)
</verification>

<success_criteria>
- NLP.js correctly classifies the 4 key test utterances (INTENT-01 through INTENT-04)
- Classification latency < 50ms after warm start (INTENT-06)
- Training corpus covers all 20 intent types with 150+ utterances
- Classifier is a reusable module ready for integration
</success_criteria>

<output>
After completion, create `.planning/phases/12.1.1-nlp-js-intent-classifier/12.1.1-01-SUMMARY.md`
</output>
