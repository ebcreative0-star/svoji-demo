---
phase: 12.1.1-nlp-js-intent-classifier
verified: 2026-03-18T00:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 12.1.1: NLP.js Intent Classifier Verification Report

**Phase Goal:** Replace Haiku API classifier with locally-trained NLP.js model for deterministic, free, fast intent classification with Czech language support. Train on existing 50+ examples from intent-classifier.ts. Keep Sonnet for chat responses only.
**Verified:** 2026-03-18
**Status:** PASSED
**Re-verification:** No -- initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | "pridej datum k fotografovi" classifies as checklist_update | VERIFIED | Test INTENT-01 PASS (live run: confidence 1.000) |
| 2 | "zaplatil jsem kameramana 15000" classifies as budget_mark_paid | VERIFIED | Test INTENT-02 PASS (live run: confidence 1.000) |
| 3 | "co mam v checklistu?" classifies as checklist_query | VERIFIED | Test INTENT-03 PASS (live run: confidence 1.000) |
| 4 | "kolik hostu potvrdilo?" classifies as guest_query | VERIFIED | Test INTENT-04 PASS (live run: confidence 1.000) |
| 5 | No Haiku API calls for intent classification (zero cost for classification) | VERIFIED | Test INTENT-05 PASS -- fetch spy confirms 0 calls for high-confidence NLP.js results |
| 6 | Classification latency < 50ms (local inference) | VERIFIED | Test INTENT-06 PASS -- warm-start classification well under 50ms |

**Score:** 6/6 truths verified. Full test suite run live: 10/10 passed, exit 0.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/ai/nlp-classifier.ts` | NLP.js singleton with train/classify API | VERIFIED | 67 lines; exports `getNlpClassifier()` and `NlpClassifier`; `import 'server-only'` guard present; `result.score` mapping correct |
| `src/lib/ai/training-data.json` | NLP.js corpus with Czech utterances | VERIFIED | 420 utterances across 21 intents (target was 150-200; 2x exceeded); all 21 intent names present |
| `src/lib/ai/__tests__/nlp-classifier.test.ts` | Test scaffold covering INTENT-01 through INTENT-06 | VERIFIED | 220 lines; 10 tests covering all 6 INTENT requirements plus integration tests; uses bare assert + process.exit(1) pattern |
| `src/lib/ai/intent-classifier.ts` | NLP.js primary + Haiku fallback classifier | VERIFIED | 461 lines; NLP.js primary path wired; Haiku fallback retained; keywordFallback removed; confidence-override removed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `nlp-classifier.ts` | `training-data.json` | `import trainingData from './training-data.json'` | WIRED | Line 11: `import trainingData from './training-data.json'` present; `addCorpus(trainingData)` call confirmed |
| `intent-classifier.ts` | `nlp-classifier.ts` | `import { getNlpClassifier } from './nlp-classifier'` | WIRED | Line 8: import present; Line 323: `getNlpClassifier()` called inside `classifyIntent()` |
| `intent-classifier.ts` | Kilo Gateway API | `fetch` fallback-only path | WIRED | `fetch(\`${KILO_BASE_URL}/chat/completions\`, ...)` at line 371; only reached when NLP.js confidence < 0.5 or None intent |
| `route.ts` | `intent-classifier.ts` | `import { classifyIntent, ... }` | WIRED | `src/app/api/chat/route.ts` line 4: all four exports imported; line 318: `classifyIntent()` called |

---

### Requirements Coverage

The PLAN frontmatter declares INTENT-01 through INTENT-06. These IDs are defined in the phase-level RESEARCH.md and ROADMAP.md (phase 12.1.1 section). Note: the top-level REQUIREMENTS.md does not list INTENT-xx IDs -- they are tracked at the phase level in `.planning/phases/12.1-ai-update-query/REQUIREMENTS.md` and the ROADMAP.md phase entry.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INTENT-01 | 12.1.1-01 | "přidej datum k fotografovi" classifies as checklist_update | SATISFIED | checklist_update in training corpus with Czech "datum k" variants; test PASS |
| INTENT-02 | 12.1.1-01 | "zaplatil jsem kameramana 15000" classifies as budget_mark_paid | SATISFIED | budget_mark_paid in training corpus; test PASS |
| INTENT-03 | 12.1.1-01 | "co mam v checklistu?" classifies as checklist_query | SATISFIED | checklist_query in training corpus; test PASS |
| INTENT-04 | 12.1.1-01 | "kolik hostu potvrdilo?" classifies as guest_query | SATISFIED | guest_query in training corpus; test PASS |
| INTENT-05 | 12.1.1-02 | No Haiku API calls when NLP.js confidence >= 0.5 | SATISFIED | fetch spy test PASS; `isNoneIntent` guard prevents false pass-through; keywordFallback removed (grep returns 0) |
| INTENT-06 | 12.1.1-01 + 02 | Classification latency < 50ms on warm path | SATISFIED | warm-start latency test PASS; NLP.js in-memory inference ~1-5ms |

**Note on REQUIREMENTS.md:** The top-level `.planning/REQUIREMENTS.md` does not include INTENT-xx entries -- these were defined as phase-specific requirements for the 12.1.x sub-phases and are tracked in ROADMAP.md success criteria and phase research files. No orphaned requirements found for phase 12.1.1 in the top-level file.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `nlp-classifier.ts` | 26 | `private nlp: any` | Info | NLP.js dock API lacks TypeScript typings; `any` is a pragmatic choice, not a stub |
| `intent-classifier.ts` | multiple | `entities.find((e: any) => ...)` | Info | Same reason -- NLP.js entity types untyped in package |

No blocker or warning anti-patterns found. The `any` usages are isolated to NLP.js interop boundaries, not implementation shortcuts. No TODO/FIXME/placeholder comments. No empty return stubs.

---

### Human Verification Required

#### 1. Production Cold Start Latency

**Test:** Deploy to production, send the first chat message after a fresh server start. Observe server logs for NLP.js training epoch output and first-classification latency.
**Expected:** Cold start completes within ~2-3 seconds (acceptable for first request); subsequent requests under 50ms. Logs show `[Intent Classifier] NLP.js classified: ...` not `falling back to Haiku`.
**Why human:** Vercel/production environment cold start behavior depends on serverless function memory allocation and cannot be assessed from the local codebase.

---

## Summary

Phase 12.1.1 fully achieves its goal. The NLP.js classifier replaces Haiku as the primary classifier:

- All 6 INTENT success criteria pass in a live test run (10/10 tests, exit 0)
- Training corpus: 420 utterances across 21 intents (2x the 150+ target)
- `nlp-classifier.ts` is a clean singleton with proper dev hot-reload guard and `server-only` protection
- `intent-classifier.ts` is correctly wired: NLP.js primary, Haiku fallback only for None/low-confidence, `keywordFallback` removed, confidence-override removed
- `route.ts` and `action-executor.ts` required zero changes (drop-in replacement confirmed)
- One notable deviation documented in SUMMARY-02: NLP.js returns `{ intent: "None", score: 1 }` for unrecognized inputs rather than low confidence -- an `isNoneIntent` guard was added to prevent false pass-through

The only remaining verification item is production cold start latency, which requires a human to observe after deployment.

---

_Verified: 2026-03-18_
_Verifier: Claude (gsd-verifier)_
