---
phase: 06-ui-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/DashboardNav.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "Desktop nav bar shows polished styling with design system tokens"
    - "Desktop nav includes Checklist, Budget, Guests, AI Chat, Settings items plus a 'Web pro hosty' link"
    - "Mobile viewport shows a fixed bottom tab bar instead of hamburger dropdown"
    - "Dashboard content is not obscured by the bottom tab bar on mobile"
    - "Active nav item is highlighted with primary color"
  artifacts:
    - path: "src/components/dashboard/DashboardNav.tsx"
      provides: "Polished desktop nav + mobile bottom tab bar"
      min_lines: 80
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with mobile bottom padding"
      contains: "pb-20"
  key_links:
    - from: "src/components/dashboard/DashboardNav.tsx"
      to: "dashboard routes"
      via: "Next.js Link with usePathname"
      pattern: "usePathname"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/dashboard/DashboardNav.tsx"
      via: "import and render"
      pattern: "DashboardNav"
---

<objective>
Redesign the dashboard navigation: polish the desktop top nav bar and replace the mobile hamburger menu with a fixed bottom tab bar.

Purpose: The dashboard navigation needs to match the design system (OKLCH tokens, proper typography) and provide thumb-friendly mobile navigation via a bottom tab bar (CONTEXT.md locked decision).

Output: Polished DashboardNav component with dual layout (desktop top bar + mobile bottom tabs), updated dashboard layout padding.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-redesign/06-RESEARCH.md

<interfaces>
From src/components/ui/index.ts:
```typescript
export { Button, type ButtonProps } from './Button';
```

Key lucide-react icons for nav:
```typescript
import { CheckSquare, DollarSign, Users, MessageCircle, Settings, Globe } from 'lucide-react';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DashboardNav with desktop polish + mobile bottom tab bar</name>
  <files>src/components/dashboard/DashboardNav.tsx</files>
  <action>
Read the existing `DashboardNav.tsx` to understand the current structure, then rewrite:

**Desktop nav (hidden on mobile: `hidden md:flex`):**
1. Keep as a top nav bar with `fixed top-0 left-0 right-0 z-40`.
2. Left side: Svoji wordmark/logo linking to `/` (use Cormorant Garamond font class).
3. Center/right: nav items as links with icons:
   - Checklist (`/checklist`, CheckSquare icon)
   - Budget (`/budget`, DollarSign icon)
   - Guests (`/guests`, Users icon)
   - AI Chat (`/chat`, MessageCircle icon)
   - Settings (`/settings`, Settings icon)
4. Add "Web pro hosty" as a separate styled link (Globe icon) -- links to the couple's public wedding page. Use `text-[var(--color-accent)]` to differentiate it.
5. Active state: `text-[var(--color-primary)]` with a bottom border accent (`border-b-2 border-[var(--color-primary)]`).
6. Inactive state: `text-[var(--color-text-light)]` with hover to `text-[var(--color-primary)]`.
7. Background: `bg-white/95 backdrop-blur-sm border-b border-[var(--color-border)]`.
8. Use `usePathname()` from `next/navigation` for active state detection.
9. Include user section on the right: show user email or avatar placeholder, sign out button using `Button` component with `variant="ghost"` and `size="sm"`.

**Mobile bottom tab bar (`md:hidden`):**
1. Fixed bottom: `fixed bottom-0 left-0 right-0 z-40 md:hidden`.
2. Background: `bg-white border-t border-[var(--color-border)]`.
3. Safe area padding: `style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}`.
4. 5 items in a flex row with `justify-around`:
   - Checklist, Budget, Guests, AI Chat, Settings (same routes as desktop).
5. Each item: icon (w-5 h-5) + label text (text-[10px] font-medium) stacked vertically.
6. Active: `text-[var(--color-primary)]`, inactive: `text-[var(--color-text-light)]`.
7. No hamburger menu, no dropdown -- the bottom tab bar IS the mobile nav.
8. Remove the existing mobile hamburger/dropdown logic entirely.

**Mobile top bar (simplified):**
On mobile, keep a minimal top bar with just the Svoji wordmark and user avatar/sign-out. No nav items in the top bar on mobile.

The component must be `'use client'` (uses `usePathname`, potentially `useState`).

Keep the existing couple data prop if it receives one. The "Web pro hosty" link can use `couple?.slug` to build the URL (`/w/${slug}`), or hide if no slug.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -q "md:hidden" src/components/dashboard/DashboardNav.tsx && grep -q "fixed bottom-0" src/components/dashboard/DashboardNav.tsx && grep -q "usePathname" src/components/dashboard/DashboardNav.tsx && echo "PASS: Bottom tab bar present with pathname detection" || echo "FAIL"</automated>
  </verify>
  <done>DashboardNav renders a polished top nav on desktop with all nav items + "Web pro hosty" link. On mobile, a fixed bottom tab bar replaces the hamburger menu. Active states use primary color. Design system tokens applied throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard layout for mobile bottom padding</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>
Read existing `src/app/(dashboard)/layout.tsx` and add mobile bottom padding to prevent content from being obscured by the bottom tab bar:

1. Find the `<main>` element (currently has `pt-16` or similar for top nav spacing).
2. Add `pb-20 md:pb-0` to account for the 64px (h-16) bottom tab bar + safe area on mobile.
3. The final className should be something like: `pt-16 pb-20 md:pb-0` (preserve existing padding-top for the fixed top nav).
4. Do NOT change the auth check logic, Supabase client setup, or any data fetching. Layout-only change.
5. If the layout passes `couple` data to DashboardNav, keep that prop passing intact.
  </action>
  <verify>
    <automated>cd /Users/eb-vm/Documents/claw/wedding-web/svoji && grep -q "pb-20" src/app/\(dashboard\)/layout.tsx && grep -q "md:pb-0" src/app/\(dashboard\)/layout.tsx && echo "PASS: Mobile bottom padding applied" || echo "FAIL"</automated>
  </verify>
  <done>Dashboard layout has `pb-20 md:pb-0` on the main content area. Content scrolls fully visible above the mobile bottom tab bar.</done>
</task>

</tasks>

<verification>
1. Desktop: Visit /checklist at md+ viewport -- top nav bar with all items, active state on Checklist, design system colors.
2. Mobile: Visit /checklist at < md viewport -- bottom tab bar with 5 icons, no hamburger menu, content not obscured.
3. Navigation: Click each nav item on mobile -- tab bar persists, active item highlights.
4. "Web pro hosty" link visible on desktop nav.
</verification>

<success_criteria>
- Desktop nav uses OKLCH tokens, Cormorant Garamond wordmark, all 6 nav items
- Mobile bottom tab bar shows 5 items with icons and labels
- Hamburger dropdown is completely removed
- Active state detection works via usePathname
- Content has bottom padding on mobile to clear the tab bar
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-redesign/06-02-SUMMARY.md`
</output>
