---
phase: 06-ui-redesign
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/SaasFooter.tsx
autonomous: true
requirements: [UI-04]
gap_closure: true

must_haves:
  truths:
    - "SaasFooter background is visually distinct from the dark warm-brown section above it"
    - "Footer uses a lighter warm color so the boundary between last section and footer is clearly readable"
    - "All footer text remains legible against the new background (sufficient contrast)"
    - "A visible separator (border-t or top margin) divides the last content section from the footer"
  artifacts:
    - path: src/components/ui/SaasFooter.tsx
      provides: "Footer with lighter background and top separator"
  key_links:
    - from: src/components/ui/SaasFooter.tsx
      to: src/app/globals.css
      via: CSS variable reference
      pattern: "var\\(--color-"
---

<objective>
Fix SaasFooter visual separation from the dark content section above it.

Purpose: UAT Test 2 (cosmetic gap from 06-UAT.md) -- user reported the footer blends into the warm-brown section above because both use near-identical dark brown tones. The footer needs a lighter background color and a visible top separator.

Output: SaasFooter with a warm light background and dark text, clearly distinct from the FinalCTA/dark sections above it.
</objective>

<execution_context>
@/Users/eb-vm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eb-vm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-redesign/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SaasFooter with light warm background and dark text</name>
  <files>src/components/ui/SaasFooter.tsx</files>
  <action>
    The current footer uses `bg-[var(--color-primary-dark)] text-white` (dark warm brown, oklch 45%). The section immediately above it (FinalCTA) is also a dark warm brown, making them visually merge. The fix: switch to a light warm background with dark text.

    Design token options from globals.css:
    - `--color-secondary`: oklch(98.1% 0.012 80) -- warm near-white/cream
    - `--color-border`: oklch(90.8% 0.012 80) -- warm light gray
    - `--color-primary`: oklch(55% 0.045 55) -- medium warm brown

    Use `--color-secondary` (warm cream) as the new footer background. Switch all text from white to dark brown using `--color-text` and `--color-text-light`. Add a `border-t-2 border-[var(--color-border)]` at the top of the footer element for a clear visual separator.

    Specific changes to src/components/ui/SaasFooter.tsx:

    1. Line 8: Replace `bg-[var(--color-primary-dark)] text-white` with `bg-[var(--color-secondary)] text-[var(--color-text)] border-t-2 border-[var(--color-border)]`

    2. Update all `text-white` references to `text-[var(--color-text)]`:
       - Brand link (line 13): `text-white` -> `text-[var(--color-text)]`
       - Brand tagline (line 16): `text-white/70` -> `text-[var(--color-text-light)]`

    3. Update all `text-white/50` (section headings) to `text-[var(--color-text-light)]`:
       - Line 23, 48, 64: `text-white/50` -> `text-[var(--color-text-light)]`

    4. Update all `text-white/70` (link default) to `text-[var(--color-text-light)]`:
       - Lines 31, 39, 54, 70, 79: `text-white/70` -> `text-[var(--color-text-light)]`

    5. Update all `hover:text-white` to `hover:text-[var(--color-text)]`:
       - Lines 32, 40, 55, 71, 80: `hover:text-white` -> `hover:text-[var(--color-text)]`

    6. Bottom bar (line 90): `border-white/10` -> `border-[var(--color-border)]`, `text-white/50` -> `text-[var(--color-text-light)]`

    Do NOT change the HTML structure, links, icons, padding, or grid layout -- only color classes.
  </action>
  <verify>
    <automated>grep -n 'color-primary-dark\|text-white' src/components/ui/SaasFooter.tsx | wc -l</automated>
  </verify>
  <done>
    Zero matches for `color-primary-dark` or `text-white` in SaasFooter.tsx. Footer uses --color-secondary background with --color-text/--color-text-light for all text. A border-t-2 separator is present on the footer element. All links remain functional (unchanged hrefs).
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `grep -n 'color-primary-dark' src/components/ui/SaasFooter.tsx` returns no matches
2. `grep -n 'text-white' src/components/ui/SaasFooter.tsx` returns no matches
3. `grep -n 'color-secondary' src/components/ui/SaasFooter.tsx` returns the footer background class
4. `grep -n 'border-t-2' src/components/ui/SaasFooter.tsx` returns the separator on the footer element
5. Visual: footer is clearly lighter than the dark FinalCTA section above it
</verification>

<success_criteria>
- Footer background is warm cream (--color-secondary), clearly lighter than the dark warm-brown sections above
- Top border provides a crisp visual boundary between last section and footer
- All text (headings, links, copyright) uses dark color tokens with sufficient contrast on cream background
- UAT Test 2 gap from 06-UAT.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-redesign/06-06-SUMMARY.md`
</output>
